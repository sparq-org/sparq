<!-- [OPUS-4.8] SBOM gap register — epic sq-toze / bead sq-toze.12. Every open gap carries a severity,
     a remediation plan, a target, and the tracking bead. No gap is papered over. -->
# SBOM + supply-chain — gap register

Severity: **P0** blocks a perfect score on a high-value framework; **P1** needed for a perfect score;
**P2** raises maturity / completeness. Each gap is honestly recorded with a remediation plan and a
`bd` bead under epic `sq-toze`. None is silently passed in the control table.

## Open gaps

| ID | Gap | Sev | Control(s) | Remediation plan | Target | Bead |
|---|---|---|---|---|---|---|
| **GS-1** | **RESOLVED ([OPUS-4.8], sq-toze.26).** ~~Per-component Supplier Name slot (NTIA N1) not emitted (partial).~~ `cargo-cyclonedx` 0.5.9 left the dedicated `components[].supplier`/`.publisher` slot empty on all components, so the literal NTIA "Supplier Name" *slot* was unpopulated per component (only the 1.3-era `author` field was present on 144/166). **Fixed:** `scripts/sbom-normalize.jq` now derives a per-component `supplier` (CycloneDX `organizationalEntity`, the NTIA Supplier-Name slot) HONESTLY from each component's identity in the RAW cargo-cyclonedx output (classified BEFORE the bom-ref is canonicalised): a **crates.io-published crate** (`registry+…` bom-ref) → `supplier.name = "crates.io"` + the crate's `https://crates.io/crates/<name>` URL (the distributor that supplied it), with the crate's own `author` carried into `publisher` where present; a **first-party workspace crate** (`path+file…/crates/sparq-*`) → `supplier = {name:"Jesse Wright", url:["https://github.com/sparq-org/sparq"]}` (matching the VEX top-level supplier); a **vendored `[patch.crates-io]` upstream crate** (`path+file…/vendor/…`, today only `spargebra`) → `supplier.name = "crates.io"` (its supplier-of-record — **NOT** the sparq project, which would be fabrication), publisher from its author; **anything else** → supplier **OMITTED** (no fabrication, per NTIA's omit/mark-unknown guidance — none occurs today, so coverage is **100%**). Idempotent + non-destructive (never overwrites a supplier already present). Both `sparq-cli`/`sparq-server` SBOMs validate against the official CycloneDX 1.5 schema with `supplier.name` on **every** component (174/174, 166/166), purl-canonicality + lifecycles + abs-path guard all intact. **CI-gated:** the GATING job `supply-chain.yml#sbom-supplier` (`scripts/check-sbom-supplier.py` + self-test `scripts/tests/test_sbom_supplier.py`) regenerates+normalizes the SBOM and asserts every cargo component carries a supplier, so a future tool bump that silently stops the derivation FAILS the PR. | P2 | N1 | **DONE** — `scripts/sbom-normalize.jq` (`derive_supplier`/`add_supplier`); CI backstop `supply-chain.yml#sbom-supplier`. | landed | `sq-toze.26` |
| **GS-2** | **Reproducible-build CHARACTERISED (GX-8), enforcement remaining.** The SBOM↔binary link is asserted via SLSA provenance (SIG-1) + the embedded cargo-auditable manifest (DEP-5); an auditor still cannot rebuild a **bit-identical** binary, but the honest reproducibility statement now exists: a measured double-build of `sparq-cli` is **byte-identical apart from 22 bytes**, all from **one** non-determinism source (the C-compiled `mimalloc` `__DATE__`/`__TIME__` `.rodata` banner + the build-id it perturbs) — [`../slsa/reproducible-build.md`](../slsa/reproducible-build.md). Higher SLSA levels + CRA integrity wanted this *or* an honest "not reproducible because…"; the honest reason is delivered. | P2 | INT-3 | DONE (doc): the honest non-determinism statement is recorded in [`../slsa/reproducible-build.md`](../slsa/reproducible-build.md) (named source = the `mimalloc` build-time banner). REMAINING: `SOURCE_DATE_EPOCH`/feature-drop + a CI rebuild-and-diff ratchet for a bit-identical claim. Shared with the SLSA + CRA + SSDF + OpenSSF worktrees. | doc DONE; CI ratchet remaining (aligned with `slsa`) | `sq-toze.9` (GX-8) |
| **GS-3** | **RESOLVED ([OPUS-4.8], sq-toze.27).** ~~No dedicated npm/JS-lockfile SBOM for the WASM client.~~ The Rust tree was SBOM'd but the published npm surface (`js/` = `@sparq-org/sparq`, the WASM client) was not — an un-SBOM'd supply-chain surface. **Fixed:** `scripts/gen-js-sbom.sh` generates a CycloneDX **1.5** SBOM for the `js/` lockfile via the pinned `@cyclonedx/cyclonedx-npm@5.0.0` (`--package-lock-only`, deterministic from the committed lockfile). Two views are shipped: `sparq-js-<ver>.sbom.cdx.json` (runtime tree, `--omit dev` — what a consumer of `@sparq-org/sparq` installs: today just `fzstd@0.1.1`) + `sparq-js-dev-<ver>.sbom.cdx.json` (full build tree incl. devDeps — `typescript`/`@types/node`/`@rdfjs/types`/`undici-types`; SSDF parity with the Rust full-tree SBOM). **Scope decision:** `js/` IN (published to npm); `site/` OUT — its `package.json` is `"private": true` (the GitHub-Pages demo, never published; its Next.js/React/bb.js tree is a dev/showcase surface covered by npm Dependabot, not a shipped artifact). Wired as `supply-chain.yml#js-sbom` (GATING; per-PR artifact) + `release.yml#sbom` (per-release; attested + attached + checksummed alongside the Rust SBOMs). Both SBOMs VALIDATED against the official CycloneDX 1.5 JSON schema (cyclonedx-npm `--validate` default-on + independent jsonschema check — see `evidence.md §5`). | P2 | scope (WASM client) | **DONE** — `scripts/gen-js-sbom.sh`; `supply-chain.yml#js-sbom`; `release.yml#sbom`. | landed | `sq-toze.27` |
| **GS-4** | **RESOLVED ([OPUS-4.8], sq-toze.28).** ~~Generated SBOM is CycloneDX 1.3~~ (was: cargo-cyclonedx default 1.3 while the VEX is 1.5 — mixed spec versions are valid but 1.5/1.6 carries richer slots). **Fixed:** both SBOM call-sites now pass `cargo cyclonedx … --spec-version 1.5` (native; cargo-cyclonedx 0.5.9 accepts `1.3`/`1.4`/`1.5`), so the published SBOM and the VEX are both CycloneDX **1.5**. `scripts/sbom-normalize.jq` additionally populates the 1.5-only `metadata.lifecycles` slot with the single honestly-assertable phase, `build` (the BOM is produced from the fully-resolved dependency tree during the build). Validated against the official CycloneDX 1.5 JSON schema (both `sparq-cli`/`sparq-server` SBOMs: VALID, lifecycles present, 0 host-path leaks, dependency graph fully resolves, normalizer idempotent). 1.6 not adopted: the pinned tool tops out at 1.5, and no 1.6-only field could be populated honestly without fabrication. | P2 | CDX-3 | **DONE** — `--spec-version 1.5` in `scripts/gen-sbom-vex.sh` + `supply-chain.yml#sbom`; `metadata.lifecycles` via `scripts/sbom-normalize.jq`. | landed | `sq-toze.28` |
| **GS-5** | **RESOLVED ([OPUS-4.8], sq-toze.29).** ~~VEX ↔ deny.toml sync is enforced by comment + manual inspection, not CI.~~ The published VEX (`supply-chain/vex.cdx.json` `vulnerabilities[].id`) and the actual cargo-deny gate (`deny.toml [advisories].ignore`) were kept in step by a "KEEP THIS IN SYNC" comment + manual review; a future one-sided edit would not have been caught automatically. **Fixed:** `scripts/check-vex-deny-drift.py` extracts the advisory-id set from BOTH sides — deny.toml via `tomllib` (robust to the `{ id, reason }` ignore form; reason prose can't be mis-read as an id, unlike a grep) and the VEX via `json` — and asserts SET EQUALITY, exiting non-zero on any unjustified difference and printing the diff (a `JUSTIFIED_DRIFT` allow-list, empty today, lets a deliberate one-sided entry be recorded with a reason rather than silencing the check). **Source-of-truth direction:** deny.toml is authoritative (it's the list cargo-deny actually enforces); the VEX must carry exactly one `vulnerabilities[].id` per ignored advisory. Wired as the GATING job `.github/workflows/supply-chain.yml#vex-deny-sync` (name has no `advisory`/`informational` whole word, so ci-summary gates it), which also runs the hermetic self-test `scripts/tests/test_vex_deny_drift.py`. **Were they in sync?** YES — both = `{RUSTSEC-2024-0436, RUSTSEC-2025-0134}` at the time this landed; no drift to resolve. (The tolerated-advisory set has changed since — the gate asserts set-equality whatever the set is, so this row is not a running inventory; the current set is recorded once in `controls/sbom.md` VEX-2/VEX-3.) Negative test: removing either deny.toml ignore makes the check exit 1 with the offending id named. | P2 | VEX-3 | **DONE** — `scripts/check-vex-deny-drift.py` + self-test + `supply-chain.yml#vex-deny-sync`. | landed | `sq-toze.29` |
| **GS-6** | **RESOLVED ([OPUS-4.8], sq-toze.30 + sq-uujh).** ~~SBOM root identity leaks the absolute build-machine path.~~ cargo-cyclonedx 0.5.9 *raw* output stamped the absolute build dir into the root `metadata.component.bom-ref` (`path+file:///<abs-path>/crates/sparq-server#0.1.0`, root `purl` = `…?download_url=file://.`) and every workspace-member ref, so a published SBOM would have carried the **CI runner's absolute path**. **Fixed:** the publication path now post-processes every shipped SBOM through `scripts/sbom-normalize.jq` — invoked from `scripts/gen-sbom-vex.sh` (the two released-binary SBOMs) and `supply-chain.yml#sbom` (every uploaded `*.cdx.json`) — which rewrites each `path+file://…#<ver>` ref to the canonical host-independent `pkg:cargo/<name>@<version>` form and strips the whole `download_url=file://…` purl qualifier **plus the trailing build-target `#src/…` subpath** (extended in sq-uujh — see GS-7), rewriting the dependency graph (`bom-ref` + every `ref`/`dependsOn` edge + nested build-target sub-components) in lock-step so internal refs still resolve. Deterministic + idempotent; both call-sites also hard-fail if any `path+file://`/`download_url=file://`/`/home/` survives. Verified locally (0 leaks, valid CycloneDX, all dep refs resolve, idempotent). *Residual (not blocking):* raw `cargo cyclonedx` still emits path refs upstream — only the post-processed publication output is sanitised. See `evidence.md §3`. **Upstream fix is deferred to bead `sq-4qo8`** — its *verification* half is now **done** (the defect persists in the latest release, `cargo-cyclonedx` 0.5.9 / 2026-03-19, no Unreleased fix; root cause pinpointed to `purl.rs#get_purl` + `generator.rs#create_component`; related upstream issue [#612](https://github.com/CycloneDX/cyclonedx-rust-cargo/issues/612)), with the ready-to-file issue/PR dossier recorded in `evidence.md §3a`. The *filing* half is owner-gated (`needs:user`). | P2 | INT-1/INT-2 (hygiene) | **DONE** (local). Upstream filing **OPEN** → `sq-4qo8` (verified-persists; dossier in `evidence.md §3a`). | landed | `sq-toze.30`, `sq-4qo8` |
| **GS-7** | **RESOLVED ([OPUS-4.8], sq-uujh).** ~~Workspace-member / build-target purls were not fully canonical.~~ After the sq-toze.30 normalizer, every workspace-member component purl was canonical `pkg:cargo/<name>@<version>` **except the root build-target sub-components** (bin/lib targets), whose purls retained a host-derived subpath fragment: `pkg:cargo/sparq-cli@0.1.0#src/main.rs`, `pkg:cargo/sparq-server@0.1.0#src/{lib,main}.rs`. `canon_purl` stripped the `?download_url=file://…` qualifier only *up to* the `#`, leaving the fragment — so these were the **only** non-canonical purls in the SBOM and produced a purl↔`bom-ref` mismatch on those rows (the `bom-ref` was already canonicalised). **Fixed:** `canon_purl` now drops the download_url qualifier together with any trailing `#subpath` (`sub("[?&]download_url=file://.*$"; "")`), gated on the workspace-local download_url marker so registry purls are byte-for-byte untouched. Verified: all member + build-target purls canonical, registry purls unchanged, idempotent, both SBOMs VALID against the official CycloneDX 1.5 schema. **Upstream:** raw cargo-cyclonedx 0.5.9 still emits these path-refs/subpath-purls — the proper fix belongs upstream; file an issue/PR against `CycloneDX/cyclonedx-rust-cargo` to emit canonical `pkg:cargo/<name>@<version>` purls for workspace/path members. This is tracked by bead **`sq-4qo8`**, whose *verification* half is **done** — the defect is confirmed to persist in the **latest** release (0.5.9, 2026-03-19; no Unreleased fix), with the root cause (`purl.rs#get_purl`'s unconditional `download_url=file://` qualifier + build-target `#src/…` subpath; `generator.rs#create_component`'s absolute `package.id` bom-ref) and a ready-to-file issue/PR dossier (cross-linking the related open upstream issue [#612]) recorded in `evidence.md §3a`; the actual upstream filing is owner-gated (`needs:user`). **Backstop ([OPUS-4.8], sq-tmyw):** since the normalizer is hand-written against the EXACT decoration shapes 0.5.9 emits, a future tool bump could re-introduce a decoration the pattern misses. The GATING job `supply-chain.yml#sbom-purl-canonical` (script `scripts/check-sbom-purl-canonical.py` + self-test `scripts/tests/test_sbom_purl_canonical.py`) regenerates+normalizes the SBOM and asserts EVERY purl matches `^pkg:cargo/[^?#]+@[^?#]+$`, so any re-decoration FAILS the PR rather than shipping. Passes on current state (all 166/174 purls canonical). | P2 | INT-1/INT-2 (hygiene) | **DONE** (local) — `scripts/sbom-normalize.jq` `canon_purl` extended; CI backstop `supply-chain.yml#sbom-purl-canonical` (sq-tmyw). Upstream filing **OPEN** → `sq-4qo8` (verified-persists in 0.5.9; dossier in `evidence.md §3a`; filing owner-gated). | landed | `sq-uujh`, `sq-tmyw`, `sq-4qo8` |

## Notable NON-gaps (recorded so the auditor does not re-open them)

These were candidate gaps in the original cross-cutting register that are now **closed** — cite as
evidence, do not re-propose:

| Was | Now | Evidence |
|---|---|---|
| **GX-1** — degraded advisories PR-gate (`continue-on-error`) | **Closed** (sq-toze.2). Advisories check is **GATING**; CVSS-4.0 blocker (sq-q8de) resolved. | `supply-chain.yml#audit` "cargo-deny check (advisories) — GATING"; `deny.toml` fail-closed. |
| **GX-2** — no checked-in / per-release SBOM with VEX | **Closed** (sq-toze.3). Per-release SBOM + version-stamped VEX, attested + attached. | `scripts/gen-sbom-vex.sh`; `supply-chain/vex.cdx.json`; `release.yml#sbom`, `#release`. |
| **GX-3** — no `.well-known/security.txt` (RFC 9116) | **Closed** (sq-toze.4). | `.well-known/security.txt` (Contact + Policy + Expires 2027-06-15). |
| **GX-7** — no cargo-auditable / cargo-vet | **Implemented & verified; bead `sq-toze.8` CLOSED.** Both are **wired**: cargo-vet is a GATING CI job (operating-verified on every PR), cargo-auditable is used in release + Docker builds. The tracking bead `sq-toze.8` is **closed** (verified in `.beads/issues.jsonl`). | `supply-chain.yml#vet`; `release.yml#package` + `Dockerfile:L70`. (Bead `sq-toze.8` = `closed`; no further close-out action needed.) |

> **Honesty note for the auditor:** the SBOM posture is strong — of the **31 control rows**, **23 are
> implemented & verified, 6 are audit-ready** (release-gated: config-verified but not yet
> operating-verified, since no `v*` release/tag/attestation exists — verified 2026-06-15), and **1 is a
> gap** (INT-3 — N1 and CDX-3 now met). Of the original six open gap items (GS-1..6), **GS-6 (abs-path leak)
> is RESOLVED** ([OPUS-4.8], sq-toze.30 — SBOM publication normalized via `scripts/sbom-normalize.jq`)
> and **GS-4 (spec version) is RESOLVED** ([OPUS-4.8], sq-toze.28 — SBOM now emitted as CycloneDX **1.5**,
> matching the VEX, with `metadata.lifecycles` populated), and **GS-5 (drift automation) is RESOLVED**
> ([OPUS-4.8], sq-toze.29 — VEX↔deny.toml sync is now the GATING CI job `supply-chain.yml#vex-deny-sync`),
> and **GS-3 (no JS SBOM) is RESOLVED** ([OPUS-4.8], sq-toze.27 — `scripts/gen-js-sbom.sh` + CI/release
> wiring), and **GS-1 (per-component supplier name) is RESOLVED** ([OPUS-4.8], sq-toze.26 — every component
> now carries an honestly-derived `supplier` via `scripts/sbom-normalize.jq`, CI-gated by
> `supply-chain.yml#sbom-supplier`), leaving **one open**, a **P2 maturity** item (reproducibility INT-3);
> **none** is a P0/P1 integrity or transparency hole, and **none** is overclaimed
> in `controls/sbom.md`. Note GS-3 was a scope item with no control row, so the gap *items* ≠ the gap *rows*.
> A NEW backstop landed independently: **GS-6/GS-7 purl-canonicality is now CI-gated** ([OPUS-4.8],
> sq-tmyw — `supply-chain.yml#sbom-purl-canonical`), so a future tool bump cannot silently re-decorate purls.
> The one item that could look like an open
> gap — GX-7 (cargo-auditable/cargo-vet) — is **implemented & verified** and its bead `sq-toze.8` is
> **closed**; this register cites it as evidence, not a gap.
