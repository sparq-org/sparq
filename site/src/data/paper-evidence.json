{
  "_comment": "[OPUS-4.8] sq-gum8 — paper-bound EVIDENCE for the academic paper factory. Distinct from benchmarks.generated.json (which is per-commit TIMING, all environment=indicative on the dev work-box). Every record here is a DETERMINISTIC, machine-INDEPENDENT metric (recall floor / answer-safety invariant / cost-model crossover / conformance ratchet floor) lifted from a real test or the central conformance scoreboard, so environment=canonical. The honesty gate in scripts/build-papers.mjs FAILS the build if a paper-bound headline table references an environment=indicative record. To add a number a paper may cite as a headline, it MUST appear here as environment=canonical and trace to a named test or const (the `source` field).",
  "schemaVersion": 2,
  "records": {
    "filtered_ann.recall_at_10_floor": {
      "value": 0.90,
      "unit": "recall@10",
      "environment": "canonical",
      "kind": "deterministic-floor",
      "n_vectors": 20000,
      "dim": 32,
      "k": 10,
      "n_queries": 100,
      "mask_selectivity": "~0.50",
      "source": "crates/sparq-vectors/tests/filtered.rs::filtered_traversal_recall_vs_exact_on_broad_mask",
      "note": "Asserted floor: filtered-traversal recall@10 vs the EXACT-filtered ground truth (nearest_exact_filtered) is gated >= 0.90 on a 20k synthetic set under a ~50% mask. Machine-independent (a deterministic seed + an assertion threshold), not a timing."
    },
    "ann.recall_at_10_floor": {
      "value": 0.95,
      "unit": "recall@10",
      "environment": "canonical",
      "kind": "deterministic-floor",
      "n_vectors": 50000,
      "dim": 32,
      "k": 10,
      "n_queries": 100,
      "source": "crates/sparq-vectors/tests/recall.rs::hnsw_recall_at_10_vs_brute_force_on_50k",
      "note": "Asserted floor: unfiltered HNSW recall@10 vs exact brute force is gated >= 0.95 on a 50k random set. The underlying ANN machinery is unmodified prior art; this floor bounds the approximation the filter operates over."
    },
    "filtered_ann.prefilter_equals_postfilter": {
      "value": true,
      "unit": "boolean-invariant",
      "environment": "canonical",
      "kind": "answer-safety-invariant",
      "source": "crates/sparq-vectors/tests/filtered_bgp.rs::filtered_equals_postfilter_and_is_subset",
      "note": "Proven by assert_eq: the filtered top-k is byte-identical (same ids, same order) to post-filtering the unfiltered top-k by the same BGP constraint, and is a subset of the unfiltered top-k. This is the answer-safety claim: pre-filtering narrows, never widens, and never changes the answer."
    },
    "filtered_ann.transitive_equals_postfilter": {
      "value": true,
      "unit": "boolean-invariant",
      "environment": "canonical",
      "kind": "answer-safety-invariant",
      "source": "crates/sparq-vectors/tests/filtered_bgp_transitive.rs",
      "note": "A multi-hop constraint (?node :owns ?x . ?x a :Vehicle) restricts ?node to the projection of the join-connected sub-BGP and matches post-filtering the unfiltered top-k by that same transitive constraint; a disconnected pattern does not affect the mask."
    },
    "filtered_ann.cyclic_equals_postfilter": {
      "value": true,
      "unit": "boolean-invariant",
      "environment": "canonical",
      "kind": "answer-safety-invariant",
      "source": "crates/sparq-vectors/tests/filtered_bgp_cyclic.rs",
      "note": "Connected-component mask derivation terminates (fixpoint worklist) and stays answer-equal to post-filter even when the constraining sub-BGP contains a cycle (?node :owns ?x . ?x :ownedBy ?node)."
    },
    "filtered_ann.prefilter_crossover": {
      "value": 0.5,
      "unit": "mask-selectivity-fraction",
      "environment": "canonical",
      "kind": "cost-model-crossover",
      "scatter_penalty": 2.0,
      "source": "crates/sparq-vectors/src/cost.rs::CostModel::decision_crossover_default_penalty (tests/cost_model.rs)",
      "note": "Deterministic decision rule (not a measured latency): pre-filter is chosen iff m*scatter_penalty <= n, i.e. with the default scatter_penalty 2.0, iff the mask admits <= half the store. Whichever branch is picked the returned top-k is identical, so a mis-estimate costs throughput, never correctness."
    },
    "conformance.geosparql_topology_floor": {
      "value": 197,
      "unit": "passing-assertions",
      "environment": "canonical",
      "kind": "conformance-ratchet-floor",
      "family": "OGC GeoSPARQL",
      "source": "crates/sparq-conformance-floors/src/lib.rs::geo::OGC_TOPOLOGY_FLOOR (the single shared const; enforced as OGC_RATCHET_FLOOR in crates/sparq-geo/tests/ogc_compliance_ratchet.rs and reported by crates/sparq-conformance/src/scoreboard.rs, both by import)",
      "binding": {
        "kind": "rust-anchor",
        "file": "crates/sparq-conformance-floors/src/lib.rs",
        "anchor": "OGC_TOPOLOGY_FLOOR"
      },
      "note": "OGC GeoSPARQL topology compliance ratchet floor: a monotone lower bound on passing sf/eh/rcc8 topology + WKT/GML equivalence assertions that CI enforces and that may only RISE (raised 119 -> 158 -> 197 as coverage grew). Deterministic and machine-independent (a const a test asserts), not a timing."
    },
    "conformance.sparql_floor": {
      "value": 1229,
      "unit": "passing-assertions",
      "environment": "canonical",
      "kind": "conformance-ratchet-floor",
      "family": "W3C SPARQL",
      "source": "crates/sparq-conformance/src/scoreboard.rs::SUITES (W3C SPARQL row); enforced by ci.yml job conformance (RATCHET=1229)",
      "note": "W3C SPARQL 1.0/1.1/1.2 query+update+syntax ratchet floor (pass + documented divergence). Deterministic; CI-enforced; may only rise."
    },
    "conformance.inference_floor": {
      "value": 1967,
      "unit": "passing-assertions",
      "environment": "canonical",
      "kind": "conformance-ratchet-floor",
      "family": "W3C RDF Semantics + OWL + N3",
      "source": "crates/sparq-conformance/src/scoreboard.rs::SUITES (inference row); enforced by ci.yml job inference-conformance (RATCHET=1967)",
      "note": "Inference (rdf-mt / OWL 2 RL / N3 / entailment / rdf-turtle) ratchet floor (pass + documented divergence). Deterministic; CI-enforced; may only rise."
    },
    "conformance.shacl_core_floor": {
      "value": 98,
      "unit": "passing-assertions",
      "environment": "canonical",
      "kind": "conformance-ratchet-floor",
      "family": "W3C SHACL",
      "source": "crates/sparq-conformance-floors/src/lib.rs::shacl::CORE_FLOOR (the single shared const; enforced as BASELINE_PASS in crates/sparq-shacl/tests/w3c_core.rs and reported by scoreboard.rs, both by import)",
      "binding": { "kind": "rust-anchor", "file": "crates/sparq-conformance-floors/src/lib.rs", "anchor": "CORE_FLOOR" },
      "note": "W3C SHACL core (data-shapes sht:Validate) ratchet floor. Deterministic; CI-enforced; may only rise."
    },
    "conformance.shacl_sparql_floor": {
      "value": 5,
      "unit": "passing-assertions",
      "environment": "canonical",
      "kind": "conformance-ratchet-floor",
      "family": "W3C SHACL",
      "source": "crates/sparq-conformance-floors/src/lib.rs::shacl::SPARQL_FLOOR (the single shared const; enforced as SHACL_SPARQL_FLOOR in crates/sparq-shacl/tests/w3c_sparql.rs and reported by scoreboard.rs, both by import)",
      "binding": { "kind": "rust-anchor", "file": "crates/sparq-conformance-floors/src/lib.rs", "anchor": "SPARQL_FLOOR" },
      "note": "W3C SHACL-SPARQL (sh:sparql node + property constraint sub-suites) ratchet floor. Deterministic; CI-enforced; may only rise."
    },
    "conformance.solid_wac_floor": {
      "value": 12,
      "unit": "passing-scenarios",
      "environment": "canonical",
      "kind": "conformance-ratchet-floor",
      "family": "Solid WAC",
      "source": "crates/sparq-solid/tests/conformance_wac.rs::WAC_SCENARIO_FLOOR (mirrored in crates/sparq-conformance/src/scoreboard.rs; guarded by tests/scoreboard_floors.rs)",
      "note": "Solid Web Access Control (WAC) library-level decision-parity ratchet floor: a monotone lower bound on the number of minimal per-construct .acl scenarios whose (agent, client, mode, resource) allow/deny decisions all match the WAC spec semantics, run through materialize_wac + AuthIndex::accessible. Deterministic and machine-independent (a const a test asserts over a fixed scenario table), not a timing; CI-enforced; may only rise."
    },
    "conformance.solid_acp_floor": {
      "value": 12,
      "unit": "passing-scenarios",
      "environment": "canonical",
      "kind": "conformance-ratchet-floor",
      "family": "Solid ACP",
      "source": "crates/sparq-solid/tests/conformance_acp.rs::ACP_SCENARIO_FLOOR (mirrored in crates/sparq-conformance/src/scoreboard.rs; guarded by tests/scoreboard_floors.rs)",
      "note": "Solid Access Control Policy (ACP) library-level decision-parity ratchet floor: a monotone lower bound on the number of minimal per-construct ACR scenarios whose (agent, client, mode, resource) allow/deny decisions all match the ACP spec semantics, run through materialize_acp + AuthIndex::accessible. Deterministic and machine-independent (a const a test asserts over a fixed scenario table), not a timing; CI-enforced; may only rise."
    },
    "conformance.cross_family_total": {
      "value": 3442,
      "unit": "passing-assertions",
      "environment": "canonical",
      "kind": "conformance-ratchet-floor",
      "family": "cross-family (7 suites)",
      "source": "crates/sparq-conformance/src/scoreboard.rs::SUITES (sum of all ratchet_floor; rendered by render_scoreboard as the total row)",
      "note": "Sum of the seven cross-family conformance ratchet floors (SPARQL 1229 + inference 1967 + SHACL core 98 + SHACL-SPARQL 5 + GeoSPARQL 119 + Solid WAC 12 + Solid ACP 12). A single deterministic, CI-enforced, monotone index across crates. If any individual floor rises this total must be updated in lock-step (the same scoreboard const is the source). Updated from the prior 3418 / 5-suite value after the Solid WAC + ACP decision-parity ratchets (sq-j174) joined the central scoreboard."
    },
    "conformance.suite_count": {
      "value": 7,
      "unit": "suites",
      "environment": "canonical",
      "kind": "conformance-ratchet-floor",
      "family": "cross-family",
      "source": "crates/sparq-conformance/src/scoreboard.rs::SUITES.len()",
      "note": "Number of distinct conformance suites the central scoreboard registers and ratchets across crates (W3C SPARQL, inference, SHACL core, SHACL-SPARQL, OGC GeoSPARQL, Solid WAC decision parity, Solid ACP decision parity)."
    },
    "memsafety.unsafe_sites_total": {
      "value": 93,
      "unit": "unsafe-sites",
      "environment": "canonical",
      "kind": "unsafe-count-ratchet-ceiling",
      "family": "memory-safety attestation",
      "source": "bench/unsafe-snapshot.json::total (enforced by scripts/unsafe-gate.py --check; CI job 'unsafe-register (count ratchet)' in .github/workflows/ci.yml; per-site rows in compliance/memsafety/unsafe-register.md)",
      "binding": {
        "kind": "json-pointer",
        "file": "bench/unsafe-snapshot.json",
        "pointer": "/total"
      },
      "note": "Total first-party unsafe sites across the whole crates/ workspace, as counted by a deterministic comment-/string-stripped keyword scan (scripts/unsafe-gate.py). This is a CI-ENFORCED RATCHET CEILING: the gate fails a PR whose live count RISES above this snapshot without a matching register row + a // SAFETY: comment + a re-seed; smaller is always allowed. Deterministic and machine-independent (a source scan over committed code, not a timing). It is a COUNT/COVERAGE discipline, NOT a proof of memory safety — it bounds and documents the unsafe surface, it does not prove the absence of undefined behaviour."
    },
    "memsafety.unsafe_crates": {
      "value": 9,
      "unit": "crates",
      "environment": "canonical",
      "kind": "unsafe-surface-partition",
      "family": "memory-safety attestation",
      "source": "bench/unsafe-snapshot.json::crates (keys: sparq-core, sparq-vectors, sparq-engine, sparq-py, sparq-cli, sparq-zk-compose, sparq-bench, sparq-lws-core, sparq-lws-wasm); accounting in compliance/memsafety/evidence.md MS-1",
      "binding": {
        "kind": "doc-anchor",
        "file": "compliance/memsafety/evidence.md",
        "quote": "**9** are unsafe-bearing"
      },
      "note": "[GPT-5.6] Number of first-party crates carrying at least one counted unsafe site in bench/unsafe-snapshot.json. Two of the nine (sparq-py and sparq-lws-core) ALSO carry a source forbid(unsafe_code): sparq-py's sites are arrow-feature-gated (conditional forbid), and sparq-lws-core's are EXAMPLE-only counting allocators (sq-gg0qq.2). sparq-engine is now an unsafe-bearing library because sq-kq9ia adds four shipped cancellation-pointer sites; its four test-only allocator sites remain registered too. [SONNET-4.6] sq-wubkf adds sparq-lws-wasm as the ninth: its four sites are a SHIPPING bounded #[global_allocator], so that crate's root is deny(unsafe_code) plus one #[allow(unsafe_code)] module rather than forbid — it is the one unsafe-bearing crate deliberately absent from the forbid set. The count is a machine-independent partition of the workspace, not a measurement. NOTE: doc-anchor is existence-strength only (the quote must be present verbatim in evidence.md — it does not parse the number); the snapshot key count is the machine source of truth."
    },
    "memsafety.forbid_crates": {
      "value": 50,
      "unit": "crates",
      "environment": "canonical",
      "kind": "unsafe-surface-partition",
      "family": "memory-safety attestation",
      "source": "compliance/memsafety/evidence.md MS-1 (grep -rl 'forbid(unsafe_code)' crates/ --include='*.rs' | sed 's#crates/##;s#/.*##' | sort -u | wc -l); 50 of 60 workspace crates",
      "binding": {
        "kind": "doc-anchor",
        "file": "compliance/memsafety/evidence.md",
        "quote": "→ **50 crates** carry a `forbid(unsafe_code)` in their source"
      },
      "note": "[GPT-5.6] Number of first-party crates carrying a source forbid(unsafe_code); sparq-py is conditional — forbid without the arrow feature, deny-level with it. Deterministic and machine-independent (a de-duplicated source grep over committed code). The forbid set (50) and the unsafe-bearing set (8) OVERLAP on sparq-py + sparq-lws-core, so they do NOT sum to the 60 total — see evidence.md MS-1. NOTE: doc-anchor is existence-strength only (verbatim quote presence); the grep in `source` is the machine source of truth."
    },
    "memsafety.total_crates": {
      "value": 60,
      "unit": "crates",
      "environment": "canonical",
      "kind": "unsafe-surface-partition",
      "family": "memory-safety attestation",
      "source": "compliance/memsafety/evidence.md MS-1 (ls -d crates/*/ | wc -l)",
      "binding": {
        "kind": "doc-anchor",
        "file": "compliance/memsafety/evidence.md",
        "quote": "ls -d crates/*/ | wc -l       # → 60 total crates"
      },
      "note": "[GPT-5.6] Total first-party crates in the workspace. Deterministic structural count. The forbid-in-src set (50) and the unsafe-bearing set (8) overlap on sparq-py + sparq-lws-core and 4 crates carry neither, so the two do NOT partition the 60 cleanly — see evidence.md MS-1. NOTE: doc-anchor is existence-strength only (verbatim quote presence); the `ls -d` count in `source` is the machine source of truth."
    },
    "memsafety.unsafe_sites_core": {
      "value": 51,
      "unit": "unsafe-sites",
      "environment": "canonical",
      "kind": "unsafe-count-ratchet-ceiling",
      "family": "memory-safety attestation",
      "source": "bench/unsafe-snapshot.json::crates['sparq-core']; register rows in compliance/memsafety/unsafe-register.md §sparq-core; enforced by scripts/unsafe-gate.py --check",
      "binding": {
        "kind": "json-pointer",
        "file": "bench/unsafe-snapshot.json",
        "pointer": "/crates/sparq-core"
      },
      "note": "Unsafe sites in sparq-core, the single crate concentrating the storage/dictionary/mmap/parallel-scatter/SIMD unsafe (threat-model boundary B5). The largest share of the 76-site total; ratchet-enforced per-crate. Deterministic source scan, not a measurement."
    },
    "cozk.reaudit_lenses": {
      "value": 4,
      "unit": "audit-lenses",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "coZK witness-validation re-audit (negative result)",
      "source": "research/mpc-cozk-reaudit.md §2 per-lens table + the four '### Lens N' sections (bead sq-9hrn); each maps a documented eprint 2025/1026 collaborative-zk-SNARK failure mode onto the intended multi-prover stack",
      "note": "Number of eprint 2025/1026 (Garg-Goel-Jain-Roberts-Sekar, CRYPTO'25) collaborative-zk-SNARK failure-mode lenses the adversarial re-audit assigns a verdict to: (1) witness-extension leakage, (2) malicious-compiler insecurity, (3) the honest-majority 'free malicious security' iff-validate-before-proving conditional, (4) the unaudited TACEO co-snarks dependency. A deterministic structural count over the committed re-audit document, not a measurement. This is NOT a soundness result: every lens is assigned RE-OPEN (not CLOSED) precisely because the collaborative path is unbuilt and cannot be certified."
    },
    "cozk.test_obligation_clauses": {
      "value": 5,
      "unit": "test-obligation-clauses",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "coZK witness-validation re-audit (negative result)",
      "source": "research/mpc-cozk-reaudit.md §3 (the R-WV requirement) — T1, T2, T3, T4 + clause C; encoded in crates/sparq-mpc/src/witness_validation_tests.rs (bead sq-7leq)",
      "note": "Number of clauses in the R-WV (witness-validation-before-proving) test obligation that converts the eprint 2025/1026 precondition into an enforceable build-time gate: T1 inconsistent-share abort-before-open, T2 witness-extension leakage probe, T3 validate-before-prove is load-bearing not advisory, T4 commitment-binding of the validated witness, and clause C the construction-provenance documentary/CI assertion. A deterministic structural count over the committed re-audit + the encoded test module. The obligation is necessary, explicitly NOT claimed sufficient, and its disposition is OPEN, not met — no collaborative prover exists to validate a witness against."
    },
    "cozk.deferred_proof_methods": {
      "value": 6,
      "unit": "trait-methods",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "coZK witness-validation re-audit (negative result)",
      "source": "crates/sparq-mpc/src/proof.rs — the CollaborativeProof::{prove, verify}, Attestation::attest_source trait methods plus their stub-test counterparts, each returning MpcError::NotYetImplemented with a named gate; the deferred-prover meta-test gate_state::deferred_prover_never_proves_over_any_witness in witness_validation_tests.rs pins the fail-closed posture",
      "note": "Number of grep-counted collaborative-proof / attestation entry points in proof.rs that return MpcError::NotYetImplemented (a gate-naming fail-closed error) rather than executing — the committed evidence that the collaborative path is unbuilt. A deterministic source-scan count over committed code, not a measurement. This is the load-bearing honesty of the negative result: there is no shippable collaborative prover to forge against, so the re-audit cannot certify soundness; it can only encode the precondition as a gate that fires if a future prove() starts returning Ok(..) while R-WV is unmet."
    },
    "cozk.single_prover_audit_issues": {
      "value": 12,
      "unit": "confirmed-issues",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "coZK witness-validation re-audit (negative result)",
      "source": "research/zk-soundness-audit.md 'Confirmed Issues' — the 12 numbered confirmed findings of the prior single-prover verifier soundness audit (the estate the collaborative path would plug into); the external single-prover audit gate is bead sq-qhy4",
      "note": "Number of confirmed soundness findings in the prior adversarial audit of the single-prover ZK verifier — the estate the collaborative multi-prover path would build on top of. Cited only to establish that the foundation is itself under an open external-audit gate (sq-qhy4): the single-prover audit was internally re-audited 'sound-as-landed for its threat model' after a v1 documented NOT sound, but no accredited external cryptographer has reviewed it, and sq-qhy4 audits the single-prover verifier only — it does NOT discharge the multi-prover construction this re-audit (sq-9hrn) governs. A deterministic structural count over the committed audit document, not a measurement, and not a security claim."
    },
    "policy_bridge.deny_overrides_correct": {
      "value": true,
      "unit": "boolean-invariant",
      "environment": "canonical",
      "kind": "answer-safety-invariant",
      "family": "ODRL access-control bridge",
      "source": "crates/sparq-solid/tests/odrl_bridge.rs::deny_overrides_allow_through_enforcement (and permit_plus_prohibition_same_subject_is_denied)",
      "note": "Proven by assertion: when a matched ODRL Permission and a matched ODRL Prohibition both apply to the same principal, action, and target, the materialised deny carved into the auth view wins through the unchanged enforcement layer (which computes union-of-allow minus union-of-deny), so the principal's accessible set for that mode is empty. This realises ODRL's `prohibit` conflict strategy structurally (the bridge re-uses the existing access-control engine rather than adding one); note the bridge does NOT consult `odrl:conflict` — per ODRL IM 2.2 the default when the property is unset is `invalid`, and the `perm`/`invalid` strategies are not representable by set subtraction (disclosed as a first-class limitation in the paper). A deterministic boolean invariant, not a timing, and not a security claim about the wider system."
    },
    "policy_bridge.fail_closed_deny_retraction": {
      "value": true,
      "unit": "boolean-invariant",
      "environment": "canonical",
      "kind": "answer-safety-invariant",
      "family": "ODRL access-control bridge",
      "source": "crates/sparq-solid/tests/odrl_bridge.rs::ambiguous_prohibition_reeval_keeps_deny_fail_closed (and definitely_lapsed_prohibition_retracts_deny)",
      "note": "Proven by assertion on the refresh path: a materialised deny is retracted only when re-evaluation proves the underlying prohibition is definitely withdrawn; an ambiguous (unprovable) re-evaluation KEEPS the deny so access is never silently widened on uncertainty. This asymmetric, fail-closed retraction rule (three-valued: Applies / Ambiguous / Withdrawn) is a deterministic boolean invariant, not a timing."
    },
    "policy_bridge.conditional_grant_rechecked": {
      "value": true,
      "unit": "boolean-invariant",
      "environment": "canonical",
      "kind": "answer-safety-invariant",
      "family": "ODRL access-control bridge",
      "source": "crates/sparq-solid/tests/odrl_bridge.rs::recipient_constraint_persists_as_rechecked_condition",
      "note": "Proven by assertion: a faithfully-mappable ODRL recipient constraint is persisted as an ACP conditional grant that is re-checked per session, so only the named recipient is granted (and the materialising party is not auto-granted). An unmappable constraint (for example a purpose constraint) falls back to a one-shot evaluation rather than being mis-encoded as a re-checked condition. A deterministic boolean invariant, not a timing."
    },
    "policy_bridge.count_enforcement_atomic": {
      "value": true,
      "unit": "boolean-invariant",
      "environment": "canonical",
      "kind": "answer-safety-invariant",
      "family": "ODRL access-control bridge",
      "source": "crates/sparq-policy/tests/odrl_count.rs::lteq_grants_n_then_denies (and base_deny_consumes_nothing, budget_isolated_per_party_and_target)",
      "note": "Proven by assertion behind the opt-in count-enforcement feature: a stateful odrl:count constraint grants exactly up to its limit and then denies, consuming exactly one unit of budget per grant and zero on a denial, with budgets isolated per (rule, party, target). A deterministic boolean invariant over an integer counter, not a timing."
    },
    "fo_km.task_count": {
      "value": 16,
      "unit": "km-questions",
      "environment": "canonical",
      "kind": "corpus-structural-count",
      "family": "FO-KM pilot",
      "source": "bench/fo-km/tasks.jsonl (one task per line; schema checked by bench/fo-km/validate_tasks.py)",
      "note": "Size of the committed knowledge-management task corpus: a deterministic, machine-independent line count over a git-tracked fixture, not a measurement and not an accuracy claim. [FABLE-5 sq-gum8.3-fokm]"
    },
    "fo_km.condition_count": {
      "value": 4,
      "unit": "ontology-conditions",
      "environment": "canonical",
      "kind": "corpus-structural-count",
      "family": "FO-KM pilot",
      "source": "bench/fo-km/overlays/ (no-fo.ttl, schema-org.ttl, gufo.ttl, dolce-dul.ttl)",
      "note": "Number of committed typing-overlay conditions the pilot compares over the same instance graph: a deterministic count of git-tracked overlay fixtures, not a measurement. [FABLE-5 sq-gum8.3-fokm]"
    },
    "fo_km.accuracy_schema_org": {
      "value": 0.84,
      "unit": "answer-accuracy",
      "environment": "indicative",
      "kind": "heuristic-graded-single-run",
      "family": "FO-KM pilot",
      "source": "bench/fo-km/RESULTS.md ('The measured result' table; deterministic heuristic grading via bench/fo-km/analyze.py)",
      "note": "schema.org-as-top condition: task-kind-weighted graded-correct fraction of the 16-task corpus in ONE fully-crossed run (one fresh Claude Haiku agent instance per condition-task pair); abstained on 2/16 tasks (abstentions scored separately, not as wrong). NON-canonical by construction: the answering agent is an LLM (non-deterministic, model- and date-dependent) and the run executed on the dev work-box; grading itself is deterministic (no model in the grading loop). Never a headline; indicative callout only. Re-verified against bench/fo-km/RESULTS.md 2026-07-01 (PR #1335 revision). [FABLE-5 sq-gum8.3-fokm]"
    },
    "fo_km.accuracy_no_fo": {
      "value": 0.58,
      "unit": "answer-accuracy",
      "environment": "indicative",
      "kind": "heuristic-graded-single-run",
      "family": "FO-KM pilot",
      "source": "bench/fo-km/RESULTS.md ('The measured result' table; deterministic heuristic grading via bench/fo-km/analyze.py)",
      "note": "Untyped (no formal ontology) incumbent-baseline condition, same run protocol as the other conditions; abstained on 5/16 tasks (the corpus is FO-exercising by construction — some tasks the untyped graph genuinely cannot answer). CORRECTED in the PR #1335 revision: the first draft mis-transcribed this baseline as 0.64 (DOLCE's figure); the canonical record value is 0.58. NON-canonical (single run, dev work-box); indicative callout only. [FABLE-5 sq-gum8.3-fokm]"
    },
    "fo_km.accuracy_dolce": {
      "value": 0.64,
      "unit": "answer-accuracy",
      "environment": "indicative",
      "kind": "heuristic-graded-single-run",
      "family": "FO-KM pilot",
      "source": "bench/fo-km/RESULTS.md ('The measured result' table; deterministic heuristic grading via bench/fo-km/analyze.py)",
      "note": "DOLCE (via DUL overlay) condition; abstained on 1/16 tasks (the lowest abstention rate of the four conditions). Beats the untyped 0.58 baseline by +0.06 — a formally rich overlay that DID convert into accuracy, which qualifies any strong fluency-only reading. Verified against bench/fo-km/RESULTS.md 2026-07-01 (PR #1335 revision). NON-canonical (single run, dev work-box); indicative callout only. [FABLE-5 sq-gum8.3-fokm]"
    },
    "fo_km.accuracy_gufo": {
      "value": 0.54,
      "unit": "answer-accuracy",
      "environment": "indicative",
      "kind": "heuristic-graded-single-run",
      "family": "FO-KM pilot",
      "source": "bench/fo-km/RESULTS.md ('The measured result' table; deterministic heuristic grading via bench/fo-km/analyze.py)",
      "note": "gUFO condition — a SINGLE figure; abstained on 2/16 tasks. The only condition below the untyped 0.58 baseline. The first draft's 'accuracy_gufo_lo/_hi' pair (a 0.54-0.58 'range') was a digest artifact: the 0.58 was the no-FO baseline mis-attributed to gUFO; no second gUFO figure exists anywhere in bench/fo-km. Corrected in the PR #1335 revision, verified against RESULTS.md 2026-07-01. NON-canonical (single run, dev work-box); indicative callout only. [FABLE-5 sq-gum8.3-fokm]"
    },
    "fo_km.abstain_no_fo": {
      "value": 5,
      "unit": "abstained-tasks",
      "environment": "indicative",
      "kind": "single-run-outcome-count",
      "family": "FO-KM pilot",
      "source": "bench/fo-km/RESULTS.md ('The measured result' table, abstain column)",
      "note": "Tasks (of 16) on which the untyped no-FO condition honestly abstained in the single fully-crossed run. Abstentions are scored separately, never as wrong answers. The highest abstention rate of the four conditions — largely by corpus construction (FO-exercising tasks the untyped graph cannot answer). Indicative single-run outcome, not canonical. [FABLE-5 sq-gum8.3-fokm]"
    },
    "fo_km.abstain_gufo": {
      "value": 2,
      "unit": "abstained-tasks",
      "environment": "indicative",
      "kind": "single-run-outcome-count",
      "family": "FO-KM pilot",
      "source": "bench/fo-km/RESULTS.md ('The measured result' table, abstain column)",
      "note": "Tasks (of 16) on which the gUFO condition abstained in the single fully-crossed run. Indicative single-run outcome, not canonical. [FABLE-5 sq-gum8.3-fokm]"
    },
    "fo_km.abstain_dolce": {
      "value": 1,
      "unit": "abstained-tasks",
      "environment": "indicative",
      "kind": "single-run-outcome-count",
      "family": "FO-KM pilot",
      "source": "bench/fo-km/RESULTS.md ('The measured result' table, abstain column)",
      "note": "Tasks (of 16) on which the DOLCE-DUL condition abstained in the single fully-crossed run — the lowest of the four conditions. Indicative single-run outcome, not canonical. [FABLE-5 sq-gum8.3-fokm]"
    },
    "fo_km.abstain_schema_org": {
      "value": 2,
      "unit": "abstained-tasks",
      "environment": "indicative",
      "kind": "single-run-outcome-count",
      "family": "FO-KM pilot",
      "source": "bench/fo-km/RESULTS.md ('The measured result' table, abstain column)",
      "note": "Tasks (of 16) on which the schema.org-as-top condition abstained in the single fully-crossed run. Indicative single-run outcome, not canonical. [FABLE-5 sq-gum8.3-fokm]"
    },
    "metamorph.selftest_oracles": {
      "value": 3,
      "unit": "oracles",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "SPARQL logic-bug testing instrument",
      "source": "crates/sparq-metamorph/tests/oracle_self_tests.rs — tlp_flags_the_seeded_wrong_result_mutant, norec_flags_the_seeded_wrong_result_mutant, differential_flags_the_seeded_wrong_result_mutant",
      "note": "Number of oracle types (TLP, NoREC, differential) whose self-tests assert non-vacuity: each must flag the seeded wrong-result mutant (FilterDropsRow, which silently removes a row from FILTER queries) as a Verdict::Violation. A deterministic structural count of test functions in the committed file; the tests run against the real InProcessSparq engine, not a mock. Campaign-dependent counts (bugs confirmed/reported/rejected) are NOT wired here — they are pending bead sq-gum8.11. [SONNET-4.6 sq-gum8.9]"
    },
    "metamorph.selftest_seeds": {
      "value": 50,
      "unit": "generator-seeds",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "SPARQL logic-bug testing instrument",
      "source": "crates/sparq-metamorph/tests/oracle_self_tests.rs::generated_cases_hold_on_pristine_sparq (for seed in 0..50)",
      "binding": { "kind": "rust-anchor", "file": "crates/sparq-metamorph/tests/oracle_self_tests.rs", "anchor": "generated_cases_hold_on_pristine_sparq" },
      "note": "Number of seeds over which the generated_cases_hold_on_pristine_sparq self-test asserts that TLP and NoREC both hold on the pristine sparq engine (both oracles must return Pass for every generated case). A deterministic range constant in the committed test file, not a measurement. Establishes that the oracle passes on the real engine before any campaign dispatches generated cases to third-party engines. [SONNET-4.6 sq-gum8.9]"
    },
    "metamorph.grammar_exclusion_seeds": {
      "value": 200,
      "unit": "generator-seeds",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "SPARQL logic-bug testing instrument",
      "source": "crates/sparq-metamorph/src/generate.rs::generated_predicates_respect_the_exclusion_list (for seed in 0..200)",
      "binding": { "kind": "rust-anchor", "file": "crates/sparq-metamorph/src/generate.rs", "anchor": "generated_predicates_respect_the_exclusion_list" },
      "note": "Number of seeds over which the grammar exclusion-list check asserts that no generated predicate contains a banned non-deterministic construct (RAND, NOW, UUID, STRUUID, BNODE, EXISTS). A deterministic range constant in the committed generate.rs test; ensures the seeded generator never produces non-reproducible predicates, making every campaign ledger entry's test case reproducible from its seed alone. [SONNET-4.6 sq-gum8.9]"
    },
    "zkarch.circuit_members": {
      "value": 35,
      "unit": "compiled-circuit-members",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/tests/gate_count_snapshot.json::members (coverage enforced by tests/gate_count.rs::snapshot_covers_every_member) — count of member keys",
      "note": "Number of compiled Noir proving members in the fixed zk/compose circuit family (the scan (k,n,r) lattice, the datatype-bucketed filter lanes, the join_eq (n_a,n_b) buckets, the path_reach members, revocation, hidden-issuer, and the two hidden-holder members). A deterministic count over the committed, regression-gated gate-count snapshot; not a measurement. Still allowlisted (unbound): a count-of-map-keys has no scalar the json-pointer tier can pin — a derived-count binding tier is F3 (sq-gum8.15) follow-up. [OPUS-4.8 sq-i6gj6: corrected 31->35 vs the live snapshot]"
    },
    "zkarch.circuit_kinds": {
      "value": 14,
      "unit": "circuit-identifier-kinds",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/src/manifest.rs::CircuitId (14 enum variants: Scan, FilterInt, FilterF64, FilterSignedInt, FilterDecimal, FilterValueDl, FilterValueDlF64, FilterValueDlDecimal, RevokeUnset, HiddenIssuer, HolderPok, HolderSet, JoinEq, PathReach)",
      "note": "Number of statement kinds in the fixed circuit-identifier space the verifier re-derives; each kind instantiates one or more compiled members on its size lattice. A deterministic enum-variant count over committed code. Still allowlisted (unbound): a variant COUNT is not a literal near the enum, so the rust-anchor literal-adjacency tier cannot pin it — a derived-count binding tier is F3 (sq-gum8.15) follow-up. [OPUS-4.8 sq-i6gj6: corrected 13->14 (PathReach added) vs the live enum]"
    },
    "zkarch.binding_obligations": {
      "value": 12,
      "unit": "verifier-obligations",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/src/verifier.rs (the twelve bind_* obligation functions: bind_query_correctness, bind_attributions, bind_issuer_attestations, bind_revocation, bind_joins, bind_entailment, bind_holder_pop, bind_holder_binding, bind_hidden_revocation, bind_hidden_issuer_attestations, bind_holder_pok, bind_holder_set); glossed in site/specs/zksparql.typ section 10.3",
      "note": "Number of fail-closed binding obligations the reference verifier enforces on every manifest. A deterministic function count over committed code; enforcement quality is NOT asserted by this count (external audit gate sq-qhy4 open). [FABLE-5 sq-3kd2g.1]"
    },
    "zkarch.audit_gates": {
      "value": 4,
      "unit": "audit-gates",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "site/specs/zksparql.typ section 10.4 (public-input reconstruction; canonical verification key; issuer signature + external key set; nonce single-use and binding), each implemented in crates/sparq-zk-compose/src/verifier.rs",
      "note": "Number of cross-cutting binding checks whose individual failure would each void the intended soundness on its own; layered around backend proof verification. A deterministic structural count over the committed spec + verifier; the gates are internally re-audited, NOT externally audited (sq-qhy4 open). [FABLE-5 sq-3kd2g.1]"
    },
    "zkarch.gates_scan_min": {
      "value": 5991,
      "unit": "ultra_honk-gates",
      "environment": "canonical",
      "kind": "gate-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/tests/gate_count_snapshot.json::members['scan_k1_n16_r4'] (bb gates -s ultra_honk; regression-gated by tests/gate_count.rs, 3% tolerance)",
      "note": "Committed circuit_size baseline of the smallest scan member (k=1 graph, n=16 slots, r=4 disclosed rows). A deterministic artifact fact of the committed circuit under the pinned toolchain (bb 5.0.0-nightly.20260324, nargo 1.0.0-beta.21); gate counts of scan members vary <1% across build platforms, absorbed by the regression gate's tolerance. Not a wall-clock measurement. [FABLE-5 sq-3kd2g.1]"
    },
    "zkarch.gates_scan_max": {
      "value": 34821,
      "unit": "ultra_honk-gates",
      "environment": "canonical",
      "kind": "gate-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/tests/gate_count_snapshot.json::members['scan_k2_n64_r8'] (bb gates -s ultra_honk; regression-gated by tests/gate_count.rs, 3% tolerance)",
      "note": "Committed circuit_size baseline of the largest scan member (k=2 graphs, n=64 slots, r=8 disclosed rows). Deterministic artifact fact under the pinned toolchain; not a wall-clock measurement. [FABLE-5 sq-3kd2g.1]"
    },
    "zkarch.gates_filter_lane": {
      "value": 17416,
      "unit": "ultra_honk-gates",
      "environment": "canonical",
      "kind": "gate-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/tests/gate_count_snapshot.json::members['filter_int_d1'] (identical for filter_int_d{1,2,4}, filter_f64_d{1..4}, filter_signed_int_d{2,4}, filter_decimal_i3_f2; bb gates -s ultra_honk; regression-gated)",
      "note": "Committed circuit_size baseline shared by every manifest-composable string-canonical filter lane: the blake3 binding of the canonical literal token dominates and the token fits one 64-byte blake3 block for all digit counts <= 19, so digit count does not move the gate count. Deterministic artifact fact under the pinned toolchain. [FABLE-5 sq-3kd2g.1]"
    },
    "zkarch.gates_filter_value_dl_int": {
      "value": 3033,
      "unit": "ultra_honk-gates",
      "environment": "canonical",
      "kind": "gate-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/tests/gate_count_snapshot.json::members['filter_value_dl_int'] (bb gates -s ultra_honk; regression-gated)",
      "note": "Committed circuit_size baseline of the opt-in dual-leaf integer value-lane filter, which replaces the blake3 token binding with a value-leaf equality and carries a documented, accepted invariant downgrade (INV-VL); OFF by default and unaudited. Cited to quantify the cost of the blake3 binding, not to recommend the lane. [FABLE-5 sq-3kd2g.1]"
    },
    "zkarch.gates_join_min": {
      "value": 7025,
      "unit": "ultra_honk-gates",
      "environment": "canonical",
      "kind": "gate-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/tests/gate_count_snapshot.json::members['join_eq_na16_nb16'] (bb gates -s ultra_honk; regression-gated)",
      "note": "Committed circuit_size baseline of the smallest hidden equality-join member (both sides in the n=16 slot bucket). Deterministic artifact fact under the pinned toolchain. [FABLE-5 sq-3kd2g.1]"
    },
    "zkarch.gates_join_max": {
      "value": 18681,
      "unit": "ultra_honk-gates",
      "environment": "canonical",
      "kind": "gate-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/tests/gate_count_snapshot.json::members['join_eq_na64_nb64'] (bb gates -s ultra_honk; regression-gated)",
      "note": "Committed circuit_size baseline of the largest hidden equality-join member (both sides in the n=64 slot bucket). Deterministic artifact fact under the pinned toolchain. [FABLE-5 sq-3kd2g.1]"
    },
    "zkarch.gates_revoke": {
      "value": 899,
      "unit": "ultra_honk-gates",
      "environment": "canonical",
      "kind": "gate-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/tests/gate_count_snapshot.json::members['revoke_unset_d10'] (bb gates -s ultra_honk; regression-gated)",
      "note": "Committed circuit_size baseline of the hidden-index revocation member (Merkle inclusion + bit-unset at depth 10) - the cheapest member of the family. Deterministic artifact fact under the pinned toolchain. [FABLE-5 sq-3kd2g.1]"
    },
    "zkarch.gates_hidden_issuer": {
      "value": 24452,
      "unit": "ultra_honk-gates",
      "environment": "canonical",
      "kind": "gate-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/tests/gate_count_snapshot.json::members['hidden_issuer_d4'] (bb gates -s ultra_honk; regression-gated)",
      "note": "Committed circuit_size baseline of the hidden-issuer attestation member (in-circuit Schnorr-over-Baby-Jubjub verification + key-set Merkle membership at depth 4). Deterministic artifact fact under the pinned toolchain. [FABLE-5 sq-3kd2g.1]"
    },
    "zkarch.gates_holder_pok": {
      "value": 10334,
      "unit": "ultra_honk-gates",
      "environment": "canonical",
      "kind": "gate-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/tests/gate_count_snapshot.json::members['holder_pok'] (bb gates -s ultra_honk; regression-gated)",
      "note": "Committed circuit_size baseline of the hidden-holder proof-of-knowledge member. The hidden-holder tiers are explicitly labelled NOT yet sound in the implementation and are opt-in only; the count quantifies circuit cost, it does NOT assert the tier's security. [FABLE-5 sq-3kd2g.1]"
    },
    "zkarch.forge_findings_mapped": {
      "value": 12,
      "unit": "forge-regression-tests",
      "environment": "canonical",
      "kind": "structural-count",
      "family": "single-prover zkSPARQL architecture",
      "source": "crates/sparq-zk-compose/tests/audit_forge_map.rs (bead sq-1gir): one named forge-and-verify regression test per historical audit finding #1-#12, each constructing that finding's specific forgery and asserting the mapped verifier reject path",
      "note": "Number of historical single-prover audit findings (research/zk-soundness-audit.md) that carry a standing 1:1 forge-negative regression test pinning the remediation closed. A deterministic structural count over the committed test file; the map is internal evidence and does NOT substitute for the open external audit (sq-qhy4). [FABLE-5 sq-3kd2g.1]"
    },
    "compare.sp2b_answer_agreement": {
      "value": 12,
      "unit": "queries (of 14)",
      "environment": "canonical",
      "kind": "answer-equivalence-oracle",
      "family": "comparative benchmarking (same-box)",
      "total": 14,
      "diff": 2,
      "competitors": ["Oxigraph", "QLever", "Virtuoso"],
      "source": "bench/dashboard/competitors.json same_box_comparisons[suite='sp2b'] (git 0ab87b2a) — per-query count_crosscheck.all_agree, computed by scripts/bench/ingest-canonical-competitors.mjs from the raw envelopes at bench/canonical-competitor-results/2026-07-07/",
      "note": "Number of SP2Bench queries whose sparq solution COUNT agrees with every independent engine (Oxigraph / QLever / Virtuoso) AND the pinned expected-rows oracle. A count comparison is machine-INDEPENDENT (a deterministic answer-equivalence check, not a timing), so environment=canonical. The 2 non-agreeing queries (q08 / q12b) are recorded HONESTLY as a DIFF, never suppressed — they are the known OPTIONAL/negation optional-semantics divergences, not a sparq correctness gap; a paper citing this must present it as an answer-equivalence rate, NOT a speed claim. [FABLE-5 sq-hmd7l.28]"
    },
    "compare.watdiv_answer_agreement": {
      "value": 16,
      "unit": "queries (of 16)",
      "environment": "canonical",
      "kind": "answer-equivalence-oracle",
      "family": "comparative benchmarking (same-box)",
      "total": 16,
      "diff": 0,
      "competitors": ["Oxigraph", "QLever", "Virtuoso"],
      "source": "bench/dashboard/competitors.json same_box_comparisons[suite='watdiv'] (git 0ab87b2a) — per-query count_crosscheck.all_agree, computed by scripts/bench/ingest-canonical-competitors.mjs from the raw envelopes at bench/canonical-competitor-results/2026-07-07/",
      "note": "Number of WatDiv queries whose sparq solution COUNT agrees with every independent engine (Oxigraph / QLever / Virtuoso) AND the pinned expected-rows oracle — full agreement, 16/16. Machine-INDEPENDENT (a deterministic answer-equivalence check, not a timing), so environment=canonical. A paper citing this must present it as an answer-equivalence rate, NOT a speed claim. [FABLE-5 sq-hmd7l.28]"
    }
  }
}
