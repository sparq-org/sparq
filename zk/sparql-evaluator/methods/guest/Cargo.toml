[package]
name = "sparq-exact-guest"
version = "0.1.0"
edition = "2024"
publish = false
license = "MIT"

[workspace]

[dependencies]
risc0-zkvm = { version = "=3.0.6", default-features = false, features = ["std"] }
sparq-proved-evaluator-model = { path = "../../model", features = ["evaluate"] }

[patch.crates-io]
# [GPT-6] Exact upstream archives with narrowly recorded dependency/feature patches.
ark-relations = { path = "../../../../vendor/zk-sdk/ark-relations-0.5.1" }
spargebra = { path = "../../../../vendor/spargebra" }

[profile.release]
debug = 0
lto = "thin"
