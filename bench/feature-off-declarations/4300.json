{
  "pr": 4300,
  "date": "2026-07-28",
  "reason": "[OPUS-5] #4300: the `expr-program` lowering is entirely `#[cfg(feature = \"expr-program\")]`, but the PR's own committed feature-matrix note already records that its call-site edits in `apply_filter_scalar`/`extend_bindings` are always-compiled and therefore move the feature-OFF bytes. With the feature OFF those edits are behaviour-identical by construction: `EvalStack` is `()`, `eval_stack()` is `Default::default()`, `RowEval` holds only the `CompiledExpr` tree and its `eval` delegates verbatim to `eval_compiled`, and `map` -> `map_init` threads that zero-sized placeholder. MEASURED by rebuilding this PR's own base.sha and head.sha trees locally with the same `cargo build --profile release-wasm -p sparq-wasm --target wasm32-unknown-unknown` the leg runs (deltas reproduce the CI run exactly): base=1555522 head=1555522 bytes, size delta +0, and 54 of 1555522 bytes differ \u2014 i.e. ZERO code volume entered the default build; what moved is line-position metadata plus the reordered no-op wrapper. This declaration governs INTENT for the feature-OFF bundle only; it makes no claim about the feature-ON evaluator, whose result-equivalence is carried by tests/differentials/expr_program_differential.rs."
}
