# routing-self-tests — cheap, hermetic validation of the orchestration routing contract.
# [OPUS-4.8] #3423: routing changes must fail on the PR that introduces them, rather
# than waiting for an out-of-band retriage run or a maintainer's local invocation.
#
# The pull-request path filter covers the routing table, the three validation/planning
# scripts, ready-issues.py (which dispatch-plan.py imports and exercises), and the
# batch-merge.py <- ci_select.py import pair (#3433: the batcher reuses the selector's
# change-class allowlist, so a drift on either side must fail on ITS PR, not at the
# batcher's next live run). The workflow itself is included so edits to this gate
# validate their replacement.
#
# Gate integration: the job name contains neither "advisory" nor "informational", so
# ci-summary auto-discovers it as a gating sibling. merge_group cannot use a paths
# filter; it runs this seconds-cheap job unconditionally so the queue ref exposes the
# same gating check. No build, network access, package install, or write permission.
name: routing-self-tests

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - "orchestration/routing.toml"
      # [OPUS-5] sparq#4819 — the names jeswr/agent-account-registry reads off
      # dispatch-plan.py. `dispatch-plan.py --self-test` asserts this file against the
      # loaded module, so a PR that edits ONLY the contract (adding a name, changing a
      # wire value) must run that assertion. Without this entry the contract file would be
      # the one input to the gate that can change without the gate running — which is the
      # silent-invisibility class it exists to close.
      - "orchestration/registry-contract.toml"
      - "scripts/routing-validate.py"
      - "scripts/route-resolve.py"
      - "scripts/dispatch-plan.py"
      - "scripts/ready-issues.py"
      # [OPUS-5] PR #4925 review — THE INPUTS THE PARTITION GUARD READS FROM THE TREE.
      # `ready-issues.py`'s `workspace_roots()` derives the `area:` partition algebra from the
      # workspace manifest plus the crate directory listing, and REFUSES to plan when the two
      # disagree. Neither input was in this filter, so the one file that can turn the guard into
      # a hard PLAN stop — root `Cargo.toml` — could change with the guard's own tests never
      # running, merge green, and stop dispatch for BOTH target repositories on the next tick.
      # `crates/*/Cargo.toml` (not `crates/**`) is deliberate: what matters here is the SET of
      # crate partition roots, which changes exactly when a crate manifest is added, removed or
      # renamed. `crates/**` would fire this lane on essentially every Rust PR in a 67-crate
      # workspace for no additional coverage of that set.
      - "Cargo.toml"
      - "crates/*/Cargo.toml"
      # [OPUS-5] the bd->issues migration pair. Their --self-tests were DEAD in this repo's
      # PR CI: bd-to-issues.py's was invoked by no workflow at all, and
      # ci-close-merged-beads.py's ran only POST-merge inside bead-autoclose.yml
      # (pull_request_target: [closed], ref: main), i.e. after the change it would have
      # caught was already on main. They also share a marker regex that must not drift
      # ("keep in sync with scripts/ci-close-merged-beads.py _MARKER_RE"), so a change to
      # either must re-run BOTH here.
      - "scripts/bd-to-issues.py"
      - "scripts/ci-close-merged-beads.py"
      # batch-merge.py imports ci_select.classify_change (#3433 class-partitioned
      # batching) — run its hermetic self-test on the PR that could break either side.
      - "scripts/batch-merge.py"
      - "scripts/ci_select.py"
      # [OPUS-5] the readiness VISIBILITY suite — pins the occupancy-attribution,
      # in-progress-review and local/orchestrator-parity contracts, and pins this
      # workflow's own run block to the scripts it must invoke.
      - "scripts/tests/test_readiness_visibility.py"
      # [OPUS-5] the model-DEPRECATION guard (maintainer directive 2026-07-26: fable + opus-4.8
      # retired in favour of opus5; docs writing off haiku/sonnet onto sol). It is cross-surface
      # — a model is selected in FOUR places, not just the routing table — so each guarded
      # surface is its own trigger. Without these entries a PR could reintroduce a retired model
      # in settings.json or a workflow JS dispatch site without ever running this gate.
      - "scripts/tests/test_no_deprecated_models.py"
      - ".claude/settings.json"
      # [OPUS-5] sq-sgu1.2: EVERY harness workflow script, not only the one carrying the
      # canonical TIER table. test_no_deprecated_models.py::TestWorkflowJsDispatch scans them
      # ALL for `agent({model: ...})` dispatch values, and test_workflow_dispatch_contract.py
      # pins the schema-on-every-producing-stage / parse / durability contracts over the same
      # set — so an edit to any of them must run this gate. Without these entries the guards
      # scan files their own trigger cannot see, which is how a workflow dispatch site changes
      # without the gate that guards it ever running.
      #
      # PR #4967 review: this is a DIRECTORY glob, not an enumeration of the files that happen to
      # exist today. An enumeration cannot cover a workflow that does not exist yet — a newly
      # added fifth script matches no filter, this lane never runs, and it merges unlinked,
      # unschema'd or unparseable. Nor can the suite's own "every workflow is a path trigger"
      # assertion catch that, since it only runs when this lane does.
      - ".claude/workflows/*.js"
      # Retained alongside the glob (redundant for CI, load-bearing for a guard):
      # test_no_deprecated_models.py::test_routing_surfaces_are_path_triggers asserts this exact
      # literal is a trigger on both events.
      - ".claude/workflows/fable-architect-drain.js"
      - "scripts/tests/test_workflow_dispatch_contract.py"
      # [OPUS-5] the LABELS -> MODEL CHAIN surfaces (review of PR #4211). The `area:gui` carve-out
      # is decided in route-resolve.py from labels written by these two files, so the end-to-end
      # suite must re-run when either the role derivation or the label WRITE path changes. The
      # write path is load-bearing: a discarded --add-label failure that still sets status:ready
      # is what silently inverted the carve-out.
      - "scripts/triage.py"
      - "scripts/retriage.py"
      - ".github/workflows/triage-issue.yml"
      # [OPUS-5] the retriage CRON itself: TestRetriageCronSeam asserts this workflow
      # still passes --apply, is unconditional and keeps issues:write. Without the
      # trigger, the one edit that guard exists to catch would not run it.
      - ".github/workflows/retriage.yml"
      # [OPUS-5] #3816 — the `needs:area` park's ONLY EXIT. `triage.py` parks a no-area
      # issue and only triage-area.py can lift it, so if that cron stops applying, the
      # partition key the whole fleet keys off silently stops being assigned.
      # TestAreaClassifierCronSeam asserts the same seam (scheduled, unconditional,
      # --apply, self-tested first, issues:write) and is vacuous without this trigger.
      - ".github/workflows/triage-area.yml"
      # ...and the workflow that self-tests triage.py + retriage.py on every PR.
      - ".github/workflows/docs-quality.yml"
      # [OPUS-5] the maintainer front door (#1135 / #4145) — the curated TOML, the renderer
      # that drops resolved entries from it, and the suite that pins both plus the
      # refresh-start-here.yml wiring.
      - "orchestration/start-here.toml"
      - "scripts/render-start-here.py"
      - "scripts/tests/test_start_here_render.py"
      - ".github/workflows/refresh-start-here.yml"
      - ".github/workflows/routing-self-tests.yml"
      # [OPUS-5] sparq#5098 — the SELF-TEST WRITE GUARD's scan surface. It scans
      # `scripts/**/*.py` for a self-test that can reach a live write (the shape that posted four
      # real comments to #7/#8 from `--selftest`), so its trigger must be the same DIRECTORY GLOB
      # rather than an enumeration: the offending file, `retriage-needs-user.py`, did not exist
      # when this filter was written, and an enumeration cannot name a script that does not exist
      # yet. That is the same reasoning already applied to `.claude/workflows/*.js` above. This
      # lane is seconds-cheap and does no build, so widening it to every script is affordable.
      - "scripts/**/*.py"
      # [OPUS-5] reg#677 — PR `area:` attribution. The scheduler partitions by
      # `area:<name>`; an unlabelled PR takes the serializing `__global__` partition
      # and defers EVERY issue, so a drift in the deriver, its declared table, or the
      # labelling workflow must fail on ITS OWN PR, not at the next dispatch tick.
      - "scripts/pr-area-labels.py"
      - "ci/area-labels.toml"
      - "scripts/tests/test_pr_area_labels.py"
      - ".github/workflows/pr-area-label.yml"
  merge_group:
  push:
    branches: [main]
    paths:
      - "orchestration/routing.toml"
      # [OPUS-5] sparq#4819 — the names jeswr/agent-account-registry reads off
      # dispatch-plan.py. `dispatch-plan.py --self-test` asserts this file against the
      # loaded module, so a PR that edits ONLY the contract (adding a name, changing a
      # wire value) must run that assertion. Without this entry the contract file would be
      # the one input to the gate that can change without the gate running — which is the
      # silent-invisibility class it exists to close.
      - "orchestration/registry-contract.toml"
      - "scripts/routing-validate.py"
      - "scripts/route-resolve.py"
      - "scripts/dispatch-plan.py"
      - "scripts/ready-issues.py"
      # [OPUS-5] PR #4925 review — THE INPUTS THE PARTITION GUARD READS FROM THE TREE.
      # `ready-issues.py`'s `workspace_roots()` derives the `area:` partition algebra from the
      # workspace manifest plus the crate directory listing, and REFUSES to plan when the two
      # disagree. Neither input was in this filter, so the one file that can turn the guard into
      # a hard PLAN stop — root `Cargo.toml` — could change with the guard's own tests never
      # running, merge green, and stop dispatch for BOTH target repositories on the next tick.
      # `crates/*/Cargo.toml` (not `crates/**`) is deliberate: what matters here is the SET of
      # crate partition roots, which changes exactly when a crate manifest is added, removed or
      # renamed. `crates/**` would fire this lane on essentially every Rust PR in a 67-crate
      # workspace for no additional coverage of that set.
      - "Cargo.toml"
      - "crates/*/Cargo.toml"
      # [OPUS-5] the bd->issues migration pair. Their --self-tests were DEAD in this repo's
      # PR CI: bd-to-issues.py's was invoked by no workflow at all, and
      # ci-close-merged-beads.py's ran only POST-merge inside bead-autoclose.yml
      # (pull_request_target: [closed], ref: main), i.e. after the change it would have
      # caught was already on main. They also share a marker regex that must not drift
      # ("keep in sync with scripts/ci-close-merged-beads.py _MARKER_RE"), so a change to
      # either must re-run BOTH here.
      - "scripts/bd-to-issues.py"
      - "scripts/ci-close-merged-beads.py"
      # batch-merge.py imports ci_select.classify_change (#3433 class-partitioned
      # batching) — run its hermetic self-test on the PR that could break either side.
      - "scripts/batch-merge.py"
      - "scripts/ci_select.py"
      # [OPUS-5] the readiness VISIBILITY suite — pins the occupancy-attribution,
      # in-progress-review and local/orchestrator-parity contracts, and pins this
      # workflow's own run block to the scripts it must invoke.
      - "scripts/tests/test_readiness_visibility.py"
      # [OPUS-5] the model-DEPRECATION guard (maintainer directive 2026-07-26: fable + opus-4.8
      # retired in favour of opus5; docs writing off haiku/sonnet onto sol). It is cross-surface
      # — a model is selected in FOUR places, not just the routing table — so each guarded
      # surface is its own trigger. Without these entries a PR could reintroduce a retired model
      # in settings.json or a workflow JS dispatch site without ever running this gate.
      - "scripts/tests/test_no_deprecated_models.py"
      - ".claude/settings.json"
      # [OPUS-5] sq-sgu1.2: EVERY harness workflow script, not only the one carrying the
      # canonical TIER table. test_no_deprecated_models.py::TestWorkflowJsDispatch scans them
      # ALL for `agent({model: ...})` dispatch values, and test_workflow_dispatch_contract.py
      # pins the schema-on-every-producing-stage / parse / durability contracts over the same
      # set — so an edit to any of them must run this gate. Without these entries the guards
      # scan files their own trigger cannot see, which is how a workflow dispatch site changes
      # without the gate that guards it ever running.
      #
      # PR #4967 review: this is a DIRECTORY glob, not an enumeration of the files that happen to
      # exist today. An enumeration cannot cover a workflow that does not exist yet — a newly
      # added fifth script matches no filter, this lane never runs, and it merges unlinked,
      # unschema'd or unparseable. Nor can the suite's own "every workflow is a path trigger"
      # assertion catch that, since it only runs when this lane does.
      - ".claude/workflows/*.js"
      # Retained alongside the glob (redundant for CI, load-bearing for a guard):
      # test_no_deprecated_models.py::test_routing_surfaces_are_path_triggers asserts this exact
      # literal is a trigger on both events.
      - ".claude/workflows/fable-architect-drain.js"
      - "scripts/tests/test_workflow_dispatch_contract.py"
      # [OPUS-5] the LABELS -> MODEL CHAIN surfaces (review of PR #4211). The `area:gui` carve-out
      # is decided in route-resolve.py from labels written by these two files, so the end-to-end
      # suite must re-run when either the role derivation or the label WRITE path changes. The
      # write path is load-bearing: a discarded --add-label failure that still sets status:ready
      # is what silently inverted the carve-out.
      - "scripts/triage.py"
      - "scripts/retriage.py"
      - ".github/workflows/triage-issue.yml"
      # [OPUS-5] the retriage CRON itself: TestRetriageCronSeam asserts this workflow
      # still passes --apply, is unconditional and keeps issues:write. Without the
      # trigger, the one edit that guard exists to catch would not run it.
      - ".github/workflows/retriage.yml"
      # [OPUS-5] #3816 — the `needs:area` park's ONLY EXIT. `triage.py` parks a no-area
      # issue and only triage-area.py can lift it, so if that cron stops applying, the
      # partition key the whole fleet keys off silently stops being assigned.
      # TestAreaClassifierCronSeam asserts the same seam (scheduled, unconditional,
      # --apply, self-tested first, issues:write) and is vacuous without this trigger.
      - ".github/workflows/triage-area.yml"
      # ...and the workflow that self-tests triage.py + retriage.py on every PR.
      - ".github/workflows/docs-quality.yml"
      # [OPUS-5] the maintainer front door (#1135 / #4145) — the curated TOML, the renderer
      # that drops resolved entries from it, and the suite that pins both plus the
      # refresh-start-here.yml wiring.
      - "orchestration/start-here.toml"
      - "scripts/render-start-here.py"
      - "scripts/tests/test_start_here_render.py"
      - ".github/workflows/refresh-start-here.yml"
      - ".github/workflows/routing-self-tests.yml"
      # [OPUS-5] sparq#5098 — the SELF-TEST WRITE GUARD's scan surface. It scans
      # `scripts/**/*.py` for a self-test that can reach a live write (the shape that posted four
      # real comments to #7/#8 from `--selftest`), so its trigger must be the same DIRECTORY GLOB
      # rather than an enumeration: the offending file, `retriage-needs-user.py`, did not exist
      # when this filter was written, and an enumeration cannot name a script that does not exist
      # yet. That is the same reasoning already applied to `.claude/workflows/*.js` above. This
      # lane is seconds-cheap and does no build, so widening it to every script is affordable.
      - "scripts/**/*.py"
      # [OPUS-5] reg#677 — PR `area:` attribution. The scheduler partitions by
      # `area:<name>`; an unlabelled PR takes the serializing `__global__` partition
      # and defers EVERY issue, so a drift in the deriver, its declared table, or the
      # labelling workflow must fail on ITS OWN PR, not at the next dispatch tick.
      - "scripts/pr-area-labels.py"
      - "ci/area-labels.toml"
      - "scripts/tests/test_pr_area_labels.py"
      - ".github/workflows/pr-area-label.yml"

permissions:
  contents: read

concurrency:
  group: routing-self-tests-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Routing contract self-tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Validate routing schema and resolver/planner contracts
        run: |
          set -euo pipefail
          python3 scripts/routing-validate.py --self-test
          python3 scripts/routing-validate.py
          python3 scripts/route-resolve.py --self-test
          # [OPUS-5] ready-issues.py was in the `paths:` filter above but its --self-test was
          # NEVER invoked here, so its assertions were dead in this repo's CI and first ran
          # inside the registry's dispatch tick — where a failure breaks EVERY target at once.
          python3 scripts/ready-issues.py --self-test
          python3 scripts/dispatch-plan.py --self-test
          python3 scripts/batch-merge.py --self-test
          python3 scripts/tests/test_readiness_visibility.py
          # [OPUS-5] a deprecated model must not reappear in ANY routing position — the routing
          # table, a .claude/settings.json agent hook, a workflow JS dispatch site, or agent
          # frontmatter. This suite also pins its OWN wiring in this workflow structurally
          # (parsed YAML, not substrings), so `if: false` on the job or the step reds it.
          python3 scripts/tests/test_no_deprecated_models.py
          # [OPUS-5] sq-sgu1.2: the harness-workflow dispatch contract — every producing stage
          # of every `.claude/workflows/*.js` carries a defined, closed schema (the
          # schema-guard trap of research/fable-work-plan.md §6.3), every script still parses,
          # and each is discoverable per the durability contract. Structural only: it reads the
          # scripts, never runs or writes them (that surface is maintainer-applied).
          python3 scripts/tests/test_workflow_dispatch_contract.py
          # [OPUS-5] reg#677: the PR `area:` deriver + its YAML seam. The suite is
          # invoked HERE and that invocation is itself pinned by
          # test_pr_area_labels.py::TestSelfTestIsActuallyInvoked — a suite nobody
          # runs cannot go red.
          python3 scripts/pr-area-labels.py --self-test
          python3 scripts/tests/test_pr_area_labels.py
          # [OPUS-5] #4145: the front-door renderer. --self-test validates the SHIPPED
          # orchestration/start-here.toml (a malformed curated file must fail on ITS PR, not at the
          # next scheduled refresh of the maintainer's front door); the suite pins the drop /
          # fail-closed / truncation rules and refresh-start-here.yml's own wiring.
          # [OPUS-5] sparq#5098: no `--self-test` anywhere under scripts/ may reach a live
          # write. Static (AST) — it never imports or runs the files it scans. The suite also
          # pins its OWN wiring here structurally, so `if: false` on this job or this step reds it.
          python3 scripts/tests/test_selftest_never_writes.py
          python3 scripts/render-start-here.py --self-test
          python3 scripts/tests/test_start_here_render.py

      # [OPUS-5] The bd->issues migration family, previously ungated on a PR (see the `paths:`
      # note above). All are hermetic: stdlib only, no gh/network/token. `bd-to-issues.py
      # --self-test` carries the migration TRUST-BOUNDARY assertions (_writer_logins permission
      # contract + the migration_owned author check), which is precisely the surface that must
      # fail on the PR that weakens it rather than at the next bulk `--apply`.
      - name: Validate the bd->issues migration and autoclose contracts
        run: |
          set -euo pipefail
          python3 scripts/bd-to-issues.py --self-test
          python3 scripts/ci-close-merged-beads.py --self-test
          # [OPUS-5] #5886: the bd<->GitHub dependency-graph AUDIT tool, recovered here from the
          # unmerged branch that authored it. It is the only producer in the tree of the
          # `missing_active` / `spurious` / `cycles_bd_open` figures that #3817's Verification
          # section names, so a native-blocker discrepancy surfaced by `ready-issues.py` (whose
          # `open_blocker_count` unions the native and marker channels) now has something to run.
          # Its --self-test is hermetic by CONSTRUCTION, not by stubbing: the suite reaches only
          # bd_blocker_edges / find_cycles / bd_id_map, none of which reach the `run()` subprocess
          # seam. It pins the ORIENTATION contract this family shares — the export record
          # {issue_id: X, depends_on_id: Y} means X is BLOCKED BY Y. Verified by mutation:
          # inverting that orientation, narrowing BLOCKER_TYPES back to ("blocks",) (the
          # row-dropping bug in its module docstring), or dropping the marker-provenance check
          # each turns this command red.
          python3 scripts/audit-issue-deps.py --self-test
          # [OPUS-5] sq-2xdg / #3290: the periodic `.beads/issues.jsonl` export-sync
          # reconciler. It IMPORTS ci-close-merged-beads.py's marker regex, migration label
          # and closeable-status set rather than re-declaring them, so its self-test is also
          # the assertion that that import still resolves — a rename of the autoclose script
          # must red here, not at the next scheduled beads-sync run where the failure would be
          # a silent staleness rather than a visible break. Hermetic: pure functions only, no
          # gh, no network, no file writes (pinned by test_selftest_never_writes.py's tree
          # scan, which covers scripts/**/*.py).
          python3 scripts/beads-export-sync.py --self-test
