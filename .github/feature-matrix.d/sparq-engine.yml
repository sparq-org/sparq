# Feature-matrix fragment for `sparq-engine`.
#
# One self-contained leg mapping per opt-in feature(group). Assembled into the
# feature-matrix `opt-in-features` strategy by scripts/assemble-feature-matrix.py
# (the `setup` job emits the combined matrix as JSON consumed via fromJSON). Adding a
# leg for THIS crate edits no OTHER crate's fragment, so concurrent opt-in-feature PRs for
# DIFFERENT crates never textually conflict (bead sq-ibrze). [OPUS-4.8]
# SCOPE ([SONNET-4.6] issue #2384): adding/removing/renaming a leg changes the emitted
# leg-name set, and THAT is a TWO-FILE change — this fragment AND
# scripts/tests/feature-matrix-legnames.golden.txt. Every other edit here (features,
# test, tier, tier-reason, comments) leaves the name set alone and is a SINGLE-file
# change. See README.md in this directory.
#
# CONTRACT (gate-critical): each leg's emitted check-run NAME is `opt-in <name>`,
# discovered as a REQUIRED check by the ci-summary `gate` aggregator BY NAME. Keep
# `name` free of the whole words "advisory"/"informational" or the leg stops gating.
# Fields: name (required -> check-run name), crate (required), features (required,
# non-empty comma list), test (bool — run `cargo test` for the leg).

# sparq-engine: CS-table star-join planner, zk-trace seam, and SPARQL 1.1 federated
# SERVICE (tests use loopback only — no net).
# [OPUS-4.8] (sq-6vshe.4) The `serialize-rdf` + `streaming-serialization` legs MOVED to
# .github/feature-matrix.d/sparq-engine-serialize.yml with the RDF writer matrix they exercise
# (seam 1 of the facade split) — a `sparq-engine --features serialize-rdf` leg would now run
# ZERO writer tests, so the tests are re-targeted to their new crate there.
- name: "sparq-engine (cs-planner)"
  crate: "sparq-engine"
  features: "cs-planner"
  test: true

# [SONNET-4.6] sq-dzbg2: persistent planner statistics are entirely behind the
# default-OFF `persistent-stats` feature, including the catalog round-trip,
# corruption-rejection, deterministic-encoding, and query-equivalence tests in
# `stats::tests`. The default workspace test archive therefore compiles this module
# out. Exercise the real feature-ON path here, matching the other isolated planner
# feature legs.
- name: "sparq-engine (persistent-stats)"
  crate: "sparq-engine"
  features: "persistent-stats"
  test: true

- name: "sparq-engine (zk)"
  crate: "sparq-engine"
  features: "zk"
  test: true

# [FABLE-5] sq-7d3dj.32.2.7: runs the SPQCPRM2 format-migration cross-version storage
# differentials (v2_saved_store_is_query_equivalent_to_v1 / cross_version_resave_round_trips_
# losslessly) through the real save_compressed → open → query path in V2 emit mode. `spqcprm2`
# forwards to sparq-core/spqcprm2 (which implies mmap), so the store/open paths compile.
- name: "sparq-engine (spqcprm2)"
  crate: "sparq-engine"
  features: "spqcprm2"
  test: true

# [OPUS-4.8] (sq-6vshe.4) Seam A2: the `service` MODULE + its in-src unit tests moved to the
# internal sparq-engine-service sub-crate (see .github/feature-matrix.d/sparq-engine-service.yml).
# This engine leg STAYS (unlike the serialize legs, which fully moved) because it still runs the
# END-TO-END integration tests that exercise `exec::eval_service` through the facade —
# tests/federation_features/* (loopback ureq path) + the exec `service_exec_tests` canned-transport
# module — which did NOT move. So `cargo test -p sparq-engine --features service` still runs real
# federated tests; the moved in-src parser/egress/transport unit tests run in the new-crate leg.
- name: "sparq-engine (service)"
  crate: "sparq-engine"
  features: "service"
  test: true

# [OPUS-5] sq-lsp7k.2.2: LOCAL rows-returning SERVICE handlers. The whole
# `service_local` module, the executor's local intercept + the `service_local` in-src
# unit tests and tests/federation_features/service_local.rs are behind the default-OFF
# `service-local` feature, so ci.yml's feature-less `nextest archive` compiles them
# EMPTY — these two legs are the ONLY thing that runs them (the sq-vya1 GUARD).
# TWO legs, because the two states are genuinely different code:
#   * `service-local` ALONE compiles the `all(service-local, not(service))` dispatch arm
#     (local handlers with NO HTTP/TLS stack) and asserts an unregistered IRI is still
#     the plain "unsupported graph pattern" error.
#   * `service,service-local` compiles the intercept-then-fall-through arm and asserts
#     the SSRF egress filter still judges every registry MISS, plus that the SERVICE
#     bind-join declines for a locally-handled IRI.
- name: "sparq-engine (service-local)"
  crate: "sparq-engine"
  features: "service-local"
  test: true

- name: "sparq-engine (service + service-local)"
  crate: "sparq-engine"
  features: "service,service-local"
  test: true

# [OPUS-4.8] sq-vya1: SPARQL window functions + the custom-aggregate registry.
# The `window` module, the `AggregateFunction::Custom` eval arm and their 8 in-src
# tests (`window::tests::*` + `aggregate::tests::custom_*`/`unregistered_*`) are all
# `#[cfg(feature = "window-functions")]`, so the default build (and ci.yml's archive)
# compiled them EMPTY and no prior engine leg enabled the feature — 8 tests never ran.
# No new dep (oxrdf already direct) and no `not(feature)` divergence, so a single
# isolated leg (matching the per-feature engine legs above) covers it; verified
# `cargo nextest list -p sparq-engine --features window-functions` shows the 8 tests.
- name: "sparq-engine (window-functions)"
  crate: "sparq-engine"
  features: "window-functions"
  test: true

# [OPUS-4.8] sq-it1x: opt-in MVCC / ACID transaction isolation. The whole `txn`
# module (`src/txn.rs`, gated `pub mod txn` in lib.rs) and its in-src unit tests
# (`txn::tests::*`, 3) plus the whole-file `tests/mvcc_txn.rs` suite (13;
# `#![cfg(feature = "txn")]`) are all `#[cfg(feature = "txn")]`, so the default
# build (and ci.yml's `--workspace --all-targets` archive, which passes NO
# `--features`) compiled them EMPTY and ran ZERO of the 16 tests — the silent
# "compiled EMPTY ... tests never ran" gap this lane exists to close. No new dep
# (`txn = []`, reuses the existing snapshot/delta-overlay substrate) and no
# `not(feature)` divergence, so a single isolated leg (matching the per-feature
# engine legs above) covers it; verified `cargo test -p sparq-engine --features
# txn` runs all 16 tests and `cargo clippy -p sparq-engine --features txn
# --all-targets -- -D warnings` is clean.
- name: "sparq-engine (txn)"
  crate: "sparq-engine"
  features: "txn"
  test: true

# [OPUS-4.8] sq-bif.12: vectorized (columnar / vector-at-a-time) primitives. The
# `chunk` module (`pub mod chunk` + the `DataChunk`/`VecCmp`/`SelVec` re-exports),
# its 9 in-src unit tests AND the new end-to-end differential suite
# (`tests/vectorized_exec_differential.rs`, `#![cfg(feature = "vectorized")]`,
# which proves the columnar gather/selection kernels are multiset-equal to the
# scalar SPARQL FILTER path) are ALL `#[cfg(feature = "vectorized")]`, so the
# default build (and ci.yml's `--workspace --all-targets` archive, which passes NO
# `--features`) compiled them EMPTY and ran ZERO of them — the silent gap this lane
# exists to close, and `vectorized` had NO leg here. No new dep (`vectorized = []`,
# only sparq-core's already-direct `Graph`/`Id`) and no `not(feature=…)`
# divergence, so one isolated leg (matching the per-feature engine legs above)
# covers it; verified `cargo nextest list -p sparq-engine --features vectorized`
# shows the previously-skipped differential tests.
- name: "sparq-engine (vectorized)"
  crate: "sparq-engine"
  features: "vectorized"
  test: true

# [OPUS-4.8] sq-bif.12: the materialised-view / query-result cache. The `cache`
# module + the `ResultCache`/`CacheStats`/`is_cacheable` re-exports and the whole
# `tests/result_cache.rs` suite (`#![cfg(feature = "result-cache")]`, incl. the new
# INSERT/DELETE/UPDATE/CLEAR write-invalidation tests) are all
# `#[cfg(feature = "result-cache")]`, so the default build compiled them EMPTY and
# ran ZERO of them — `result-cache` had NO leg here (a pre-existing silent gap). No
# new dep (`result-cache = []`, std `HashMap`/`Mutex`/`Arc` only) and no
# `not(feature=…)` divergence, so one isolated leg covers it; verified `cargo
# nextest list -p sparq-engine --features result-cache` shows the result_cache tests.
- name: "sparq-engine (result-cache)"
  crate: "sparq-engine"
  features: "result-cache"
  test: true

# [OPUS-4.8] sq-xj29q: the Oxigraph-shaped per-solution accessor on QueryResult.
# `pub mod solution` (the zero-copy `QuerySolution`/`QuerySolutionIter`/
# `QuerySolutionBindings`/`VarIndex` views over the columnar `rows`) is
# `#[cfg(feature = "query-solution")]` in lib.rs, so its in-module `#[cfg(test)]
# mod tests` (6 unit tests: solutions_match_columnar_rows / missing_and_unbound_are_none
# / iter_yields_bound_pairs_only / index_returns_bound_term / index_panics_on_missing
# / empty_result_no_solutions) AND the doctest on `QueryResult::solutions` are compiled
# EMPTY by the default build (and by ci.yml's `--workspace --all-targets` archive, which
# passes NO `--features`) — so all 7 ran ZERO times and `query-solution` had NO leg here:
# the silent "compiled EMPTY ... tests never ran" gap this lane exists to close. No new
# dep (`query-solution = []`, only the already-direct oxrdf `Term`/`Variable` and the
# crate's own `QueryResult`) and no `not(feature=…)` divergence, so a single isolated leg
# (matching the per-feature engine legs above) covers it; verified `cargo test -p
# sparq-engine --features query-solution` runs all 6 unit tests + the doctest, the
# default `cargo test -p sparq-engine` shows ZERO `solution::tests`, and `cargo clippy
# -p sparq-engine --all-targets --features query-solution -- -D warnings` is clean.
- name: "sparq-engine (query-solution)"
  crate: "sparq-engine"
  features: "query-solution"
  test: true

# [OPUS-4.8] sq-rp3um: parameterized prepared queries — safe value binding to
# prevent SPARQL injection (#901). The whole `params` module (`pub mod params`:
# `PreparedQuery::bind`, `PreparedUpdate`, the `params::value::*` term ctors and
# the 16 in-src unit tests `params::tests::*` — incl. the load-bearing #901
# injection scenarios) AND the new `update`-path helpers `update_prepared` /
# `update_in_place_prepared(_with_budget)` are all `#[cfg(feature = "params")]`, so
# the default build (and ci.yml's `--workspace --all-targets` archive, which passes
# NO `--features`) compiled them EMPTY and ran ZERO of the 16 tests — the silent
# "compiled EMPTY ... tests never ran" gap this lane exists to close, and `params`
# had NO leg here. No new dep (`params = []`, only the already-direct oxrdf
# `Term`/`Variable`/`Literal`/`NamedNode`/`BlankNode` + spargebra algebra) and no
# `not(feature=…)` divergence, so a single isolated leg covers it; verified `cargo
# nextest list -p sparq-engine --features params` shows the 16 params tests and
# `cargo clippy -p sparq-engine --all-targets --features params -- -D warnings` is clean.
- name: "sparq-engine (params)"
  crate: "sparq-engine"
  features: "params"
  test: true

# [OPUS-4.8] sq-u4lgr (#902): structured EXPLAIN — the typed `PlanNode` plan tree,
# its hand-written JSON projection, per-operator q-error, and the bounded
# `SlowQueryRing`. The whole `explain_json` module (`pub mod explain_json` + the
# `PlanNode`/`SlowQuery`/`SlowQueryRing` re-exports), the trace node's `est` field,
# the planner-replay estimate helper (`exec::bgp_estimate`) and the 6 in-src unit
# tests (`explain_json::tests::*`, incl. the load-bearing q-error + JSON-round-trip +
# slow-ring-worst-by-time checks) are ALL `#[cfg(feature = "explain-json")]`, so the
# default build (and ci.yml's `--workspace --all-targets` archive, which passes NO
# `--features`) compiled them EMPTY and ran ZERO of the 6 — the silent "compiled
# EMPTY ... tests never ran" gap this lane exists to close, and `explain-json` had NO
# leg here. No new dep (`explain-json = []`; the JSON is hand-rolled and serde_json is
# a DEV-dependency only) and no `not(feature=…)` divergence, so a single isolated leg
# (matching the per-feature engine legs above) covers it; verified `cargo test -p
# sparq-engine --features explain-json explain_json` runs all 6 and `cargo clippy -p
# sparq-engine --all-targets --features explain-json -- -D warnings` is clean.
- name: "sparq-engine (explain-json)"
  crate: "sparq-engine"
  features: "explain-json"
  test: true

# [OPUS-4.8] sq-gr8mb (survey §A3): exact-bitmap semi-join reducer on dense u32 ids.
# The `semijoin` module (`src/semijoin.rs`, `pub(crate) mod semijoin` in lib.rs — the
# `KeyFilter` membership filter) + its 6 in-src unit tests (`semijoin::tests::*`) AND the
# `scan_to_bindings` prefilter parameter + its construction in `eval_bgp_binary` are ALL
# `#[cfg(feature = "semijoin-bitmap")]`, so the default build (and ci.yml's `--workspace
# --all-targets` archive, which passes NO `--features`) compiled them EMPTY. The
# end-to-end equivalence suite `tests/semijoin_differential.rs` is INTENTIONALLY NOT
# feature-gated (it runs in both states to prove on==off), but the in-src unit tests are
# gated and run ZERO times by default — the silent gap this lane closes. No new dep
# (`semijoin-bitmap = []`, only the already-direct sparq-core + rustc-hash) and no
# `not(feature=…)` divergence (the off path is byte-identical), so a single isolated leg
# (matching the per-feature engine legs above) covers it; verified `cargo test -p
# sparq-engine --features semijoin-bitmap` runs the 6 unit tests + the 5 differential
# tests and `cargo clippy -p sparq-engine --all-targets --features semijoin-bitmap
# -- -D warnings` is clean.
- name: "sparq-engine (semijoin-bitmap)"
  crate: "sparq-engine"
  features: "semijoin-bitmap"
  test: true

# [OPUS-4.8] sq-5zf8i (survey §A4): Yannakakis bottom-up full-semijoin PREPASS for acyclic
# BGPs. The prepass driver (`eval_bgp_yannakakis` + `semijoin_reduce` + the `SjRelation`
# materialised relation + the `YANNAKAKIS_MIN_REL` cost-gate in `exec.rs`) and the pure
# join-tree builder (`semijoin::build_join_tree` + `TreeNode`, with its 5 in-src tests) plus
# the 2 in-src `exec` engaged/cost-gate unit tests (`wcoj_tests::yannakakis_*`) are ALL
# `#[cfg(feature = "yannakakis")]`, so the default build (and ci.yml's `--workspace
# --all-targets` archive, which passes NO `--features`) compiled them EMPTY and ran ZERO of
# them — the silent gap this lane closes. The end-to-end equivalence suite
# `tests/yannakakis_differential.rs` is INTENTIONALLY NOT feature-gated (it runs in BOTH
# states to prove on==off), but the in-src unit tests are gated and run ZERO times by
# default. The feature IMPLIES `semijoin-bitmap` (it reuses `KeyFilter`); the off path is
# byte-identical (no `not(feature=…)` divergence). No new dep (`yannakakis =
# ["semijoin-bitmap"]`, only the already-direct sparq-core + rustc-hash; no `unsafe`), so a
# single isolated leg covers it; verified `cargo test -p sparq-engine --features yannakakis`
# runs the new unit + differential tests and `cargo clippy -p sparq-engine --all-targets
# --features yannakakis -- -D warnings` is clean.
- name: "sparq-engine (yannakakis)"
  crate: "sparq-engine"
  features: "yannakakis"
  test: true

# [FABLE-5] sq-7d3dj.30.14 (#1787): the `cluster-materialize` opt-in planner path gates ALL
# of its code (`src/cluster.rs`, the `eval_bgp_binary` hook + `eval_bgp_cluster` in exec.rs)
# and its tests — 6 in-src `cluster::tests` unit tests + the whole-file result-equivalence
# suite `tests/cluster_materialize_differential.rs` (which drives the REAL `eval_bgp_cluster`
# via the doc-hidden `with_test_thresholds` hook) — behind `#[cfg(feature = "cluster-materialize")]`.
# Without this leg the feature-ON tests (the ONLY CI executions of the cluster path) run ZERO
# times. No new dep (`cluster-materialize = []`, only the already-direct sparq-core + rustc-hash;
# no `unsafe`); the off path compiles the module away and is byte-identical, so a single isolated
# leg covers it. Verified `cargo test -p sparq-engine --features cluster-materialize` runs the
# suite and `cargo clippy -p sparq-engine --all-targets --features cluster-materialize -- -D warnings`
# is clean.
- name: "sparq-engine (cluster-materialize)"
  crate: "sparq-engine"
  features: "cluster-materialize"
  test: true

# sq-7d3dj.30.1 (#1735): the `algebra-rewrite` opt-in pass gates ALL of its unit tests
# (11 in `src/rewrite.rs`, incl. the soundness revert-witnesses
# `certain_vars_group_excludes_aggregate_output` + the anti-join decline tests) and the
# whole-file e2e result-equivalence suite `tests/rewrite_pass.rs` behind
# `#[cfg(feature = "algebra-rewrite")]` — without this leg they run ZERO times in CI
# (ci.yml's --all-features lanes are clippy/rustdoc only). Reviewer-required leg (Fable
# verdict on #1735): the regression witness for a previously-caught unsoundness must sit
# in a gate, not a one-time local run. No new dep (`algebra-rewrite = []`), feature-OFF
# path byte-identical (cfg-split PreparedQuery::parse).
- name: "sparq-engine (algebra-rewrite)"
  crate: "sparq-engine"
  features: "algebra-rewrite"
  test: true

# [OPUS-4.8] sq-iywur: opt-in DPccp dynamic-programming join-order enumerator. The whole
# `dp` module (`src/dp.rs`, `pub mod dp` in lib.rs — the DPccp csg-cmp-pair enumerator, the
# `with_dp_planner`/`with_dp_planner_budget` per-thread install, `QueryGraph`/`JoinTree`)
# with its 7 in-src unit tests (`dp::tests::*`, incl. the 4000-random-graph brute-force
# optimality check) AND the executor bridge (`eval_bgp_dp`/`eval_join_tree` + the planner
# hook in `eval_bgp_binary`) are ALL `#[cfg(feature = "dp-planner")]`, so the default build
# (and ci.yml's `--workspace --all-targets` archive, which passes NO `--features`) compiled
# them EMPTY and ran ZERO of the 7 — the silent "compiled EMPTY ... tests never ran" gap
# this lane closes. The end-to-end result-equivalence suite `tests/dp_planner_differential.rs`
# is INTENTIONALLY NOT feature-gated (it runs in BOTH states to prove DP == greedy), but the
# in-src unit tests are gated and run ZERO times by default. No new dep (`dp-planner = []`,
# only the already-direct rustc-hash; no `unsafe`) and no `not(feature=…)` divergence (the
# off path is byte-identical), so a single isolated leg covers it; verified `cargo test -p
# sparq-engine --features dp-planner` runs the 7 unit + 9 differential tests and `cargo
# clippy -p sparq-engine --all-targets --features dp-planner -- -D warnings` is clean.
- name: "sparq-engine (dp-planner)"
  crate: "sparq-engine"
  features: "dp-planner"
  test: true

# [FABLE-5] sq-7d3dj.30.7: the `value-join` opt-in equality-FILTER → value-join
# pass gates ALL of its tests behind `#[cfg(feature = "value-join")]` — the in-src
# differential suite (`exec::eqjoin::tests`: per-term-class kill-switch
# differentials incl. the sq-lr2ii high-precision-decimal witness, decline witnesses
# with a FIRED anti-vacuity counter, direct unit tests for `key_of`/`expr_total`/
# `pattern_components`) and the e2e acceptance suite `tests/eqjoin_value_join.rs`
# (q05a-shape row-identity vs an independent `!(?a != ?b)` oracle + the q12a ASK twin).
# Without this leg they run ZERO times in CI (ci.yml's --all-features lanes are
# clippy/rustdoc only). No new dep (`value-join = []`); feature-OFF path byte-identical
# (the module does not compile, the conjunctive arm is the verbatim pre-pass body).
- name: "sparq-engine (value-join)"
  crate: "sparq-engine"
  features: "value-join"
  test: true

# [FABLE-5] sq-7d3dj.30.11 (#1785): the `id-filter-fastpath` opt-in id-level `=`/`!=`
# FILTER fast path gates ALL 19 of its tests behind
# `#[cfg(all(test, feature = "id-filter-fastpath"))]` — the `idfast_unit` module in
# exec.rs (the load-bearing differential suite that pairs EVERY id kind — IRIs, bnodes,
# numeric-promotion pairs, language-tagged, ill-typed literals — and asserts the fast
# verdict equals the exact `term_of` + `values_equal` oracle row-for-row, plus the static
# non-literal analysis witnesses, the `equal_idfast` edge cases, the q08-shape e2e query,
# the EXISTS-re-entry column-set-leak regression test, and the non-vacuity mutation check).
# Without this leg they run ZERO times in CI (ci.yml's `--workspace --all-targets` archive
# passes NO `--features`, and its `--all-features` lanes are clippy/rustdoc only) — the
# silent "compiled EMPTY ... tests never ran" gap this lane closes. No new dep
# (`id-filter-fastpath = []`, only the already-direct sparq-core + rustc-hash; no `unsafe`).
# Verified `cargo nextest list -p sparq-engine --features id-filter-fastpath` shows the 19
# `idfast_unit` tests and `cargo test -p sparq-engine --features id-filter-fastpath` runs
# them green. (The feature-OFF path is NOT byte-identical to base — an always-compiled
# EXISTS-re-entry refactor moved bytes, declared in bench/feature-off-declarations/1785.json —
# but is behaviour-identical.)
- name: "sparq-engine (id-filter-fastpath)"
  crate: "sparq-engine"
  features: "id-filter-fastpath"
  test: true

# [OPUS-4.8] sq-7d3dj.30.20: the `antijoin-static-decline` opt-in fix gates ALL of its code
# (the static early-decline gate + `cond_has_seedable_correlation` in exec.rs and the
# `EARLY_DECLINED`/`record_early_decline`/`early_declined` test-stat surface) behind
# `#[cfg(feature = "antijoin-static-decline")]`, so the default build (and ci.yml's
# `--workspace --all-targets` archive, which passes NO `--features`) compiled them EMPTY.
# The whole-file result-equivalence + non-vacuity suite
# `tests/antijoin_static_decline_differential.rs` runs its equivalence + mutation tests in
# BOTH states (to prove on==off), but its non-vacuity test
# (`early_decline_fires_on_q07_not_on_q06`, asserting the gate FIRES on the bare-`!bound` q07
# shape and NOT on the correlated q06 shape) is `#[cfg(feature = "antijoin-static-decline")]`
# and runs ZERO times without this leg — the silent gap this lane closes. No new dep
# (`antijoin-static-decline = []`, only the already-direct oxrdf/spargebra + rustc-hash; no
# `unsafe`) and no `not(feature=…)` divergence (the off path is byte-identical), so a single
# isolated leg covers it; verified `cargo test -p sparq-engine --features antijoin-static-decline`
# runs the suite green and `cargo clippy -p sparq-engine --all-targets --features
# antijoin-static-decline -- -D warnings` is clean.
- name: "sparq-engine (antijoin-static-decline)"
  crate: "sparq-engine"
  features: "antijoin-static-decline"
  test: true

# [FABLE-5] sq-lsp7k.10: named parameterized SPARQL templates — the shared definition +
# typed-JSON-binding layer (`templates` module: `Template::new/from_json/to_json/bind_json`,
# `ParamType`, `Bound::{is_update,is_graph_form,render}`) behind the server's `/templates`
# REST surface and the MCP `template_invoke` tool. `templates = ["params", "dep:serde_json"]`
# — everything is `#[cfg(feature = "templates")]`, so the default build compiles it EMPTY and
# runs ZERO of the 12 `templates::tests::*` unit tests (incl. the fail-closed
# unknown/missing/mistyped-parameter contracts and the #901 injection-through-the-template
# scenario); this leg closes that gap. serde_json is already the crate's dev-dependency
# (workspace-ubiquitous), so the leg pulls no new crate.
- name: "sparq-engine (templates — named parameterized templates over params)"
  crate: "sparq-engine"
  features: "templates"
  test: true

# [FABLE-5] (sq-lsp7k.3.1, PR #2007): programmatic full-path enumeration over one
# predicate (`paths` module: `enumerate_paths`, `PathSpec`, `PathMode`, `PathSolution`).
# `paths = []` — dependency-free, OFF by default; the whole module (`pub mod paths` in
# lib.rs) and the whole-file suite `tests/paths_core.rs` (`#![cfg(feature = "paths")]`)
# are `#[cfg(feature = "paths")]`, so the default build (and ci.yml's `--workspace
# --all-targets` archive, which passes NO `--features`) compiles them EMPTY and runs
# ZERO of the tests — the silent "compiled EMPTY ... tests never ran" gap this lane
# exists to close (tripped the C1 structural guard on #2007). No new dep and no
# `not(feature=…)` divergence, so a single isolated leg covers it.
- name: "sparq-engine (paths)"
  crate: "sparq-engine"
  features: "paths"
  test: true

# [GPT-5.6] sq-lk3aw.4: exact-certified spatial FILTER pushdown is default-OFF,
# and tests/spatial_exact_pushdown.rs is compiled only with this feature.
- name: "sparq-engine (spatial-exact-pushdown)"
  crate: "sparq-engine"
  features: "spatial-exact-pushdown"
  test: true

# [FABLE-5] sq-7d3dj.11: the `expr-program` opt-in FLAT compiled scalar-expression programs
# for FILTER/BIND. The whole lowering (`exec::expr_program`, the `RowEval` program arm, the
# `expr_program_testing` toggle in lib.rs) is `#[cfg(feature = "expr-program")]`, so the
# default build — and ci.yml's `--workspace --all-targets` archive, which passes NO
# `--features` — compiles it EMPTY. Its acceptance suite
# `tests/differentials/expr_program_differential.rs` compiles in BOTH states, but WITHOUT this
# leg its load-bearing half never runs: with the feature off both legs of `tree_vs_program`
# are the same tree evaluator, so the ON==OFF row-identity differential (over SPARQL 3VL
# short-circuiting, `!error`, `IF`/`COALESCE` over a type error, `IN` with an erroring member,
# `sameTerm` vs `=`, BIND/STR term-identity passthrough, EXISTS re-entry inside a lowered
# spine, and the rayon path) is vacuous, and the FIRED anti-vacuity counter assertion
# (`expr_program_testing::lowered() > 0` on the program leg, `== 0` on the tree leg) is
# `#[cfg]`-inert. This leg is where the flat evaluator is actually exercised. No new dep
# (`expr-program = []`, no `unsafe`); with the feature off the FILTER/BIND path is the
# verbatim tree evaluator and the scratch stack is a zero-sized placeholder, but the call-site
# edits in `apply_filter_scalar`/`extend_bindings` are always-compiled, so the feature-OFF
# wasm bundle MOVES BYTES vs base (behaviour-identical — declare it under
# bench/feature-off-declarations/).
- name: "sparq-engine (expr-program)"
  crate: "sparq-engine"
  features: "expr-program"
  test: true
